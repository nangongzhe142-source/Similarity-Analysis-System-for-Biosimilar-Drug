# BioCompare 前端框架与业务流程

## 1. 页面层级与路由

| 层级 | 页面 | 路由 | 职责 |
| --- | --- | --- | --- |
| 一级 | 总项目首页 | `/project` | 整套材料统一上传、自动解析分发、一键批量运行、项目进度、专项状态概览和Word导出 |
| 一级 | 统一输入 | `/project/data` | 登记候选药/参照药文件并按专项路由 |
| 一级 | 公共规则 | `/project/rules` | 冻结公共参数、PTM区间方法和审评边界 |
| 一级 | 汇总简报 | `/project/report` | 汇总状态和客观结果，并穿透至专项明细 |
| 二级 | 专项运行 | `/modules/[moduleId]` | 单项配置、单独运行、查看执行状态 |
| 二级 | 专项结果 | `/modules/[moduleId]/results` | 展示指标、图表、明细、预警和审评备注 |

根路由 `/` 自动进入 `/project`。目前包含完整分子量、脱糖完整分子量和 PTM 位点定量三个专项。

## 2. 两种业务运行模式

### 总项目批量运行

1. 在总项目首页一次上传企业整套申报材料。
2. 系统解析CSV/TSV/XLSX，依据文件名、工作表、字段和样品角色自动分发。
3. 页面展示每个专项的候选药/参照药就绪、缺失或冲突状态。
4. 选择串行或并行调度，点击“一键执行已识别项目”。
5. 每个专项分别经历排队、执行、完成/需关注/失败状态。
6. 专项结果回传共享项目状态，不互相覆盖。
7. 总项目读取各专项摘要，可直接导出Word或穿透至二级明细页。

### 专项独立运行

1. 从侧边菜单或总项目卡片进入专项页。
2. 在专项页配置该项输入，或直接运行内置示例。
3. 点击“运行本专项”。
4. 结果写回同一项目状态，并自动出现在一级汇总报告中。
5. 进入专项结果页查看完整明细。

## 3. 任务状态机

`未启动 → 已排队 → 执行中 → 已完成 / 需关注 / 失败`

每个专项保存独立的状态、进度、运行来源、开始/完成时间和结构化结果。运行来源分为“总项目调度”和“专项独立运行”。页面跳转时由顶层 `ProjectProvider` 保持状态。

## 4. 数据流转骨架

`项目输入 → 专项输入映射 → 调度器 → 分析适配器 → 结构化专项结果 → 项目结果仓 → 汇总简报 → 专项明细`

- 页面不直接依赖具体算法，统一经过分析适配器。
- 文件按专项隔离，避免完整分子量数据被错误传给 PTM 模块。
- 总项目只消费专项摘要；二级结果页保留详细结果。
- 当前已有的 UniDec 和 PTM 区间能力继续通过适配器调用。
- 未上传文件时可运行示例数据，用于验证整个页面和数据流转链路。

## 5. 后续集成预留

接口契约集中在 `lib/integration-contracts.ts`：

- `AnalysisAdapter`：算法函数、外部软件、远程计算服务。
- `OcrAdapter`：图片/PDF文字及表格结构化。
- `ReportAdapter`：规则化报告生成及 Word/PDF 输出。
- `LlmSummaryAdapter`：根据结构化事实生成可编辑文字草稿。

OCR、LLM 和外部 API 不参与任务状态与路由控制，因此后续替换或增加实现时无需重构页面层级。

## 6. 当前监管边界

- 工具输出客观数据状态、区间符合性和风险提示。
- PTM 仍以多批参照药区间为第一判断维度，组间差值仅辅助展示。
- 不自动输出“相似”或“不相似”的最终结论。
