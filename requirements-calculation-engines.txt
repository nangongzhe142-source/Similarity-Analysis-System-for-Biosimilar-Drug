# Batch 2 Python environment (hplc-py is GPL-3.0; deploy as an isolated process)
hplc-py==0.2.8

# Sequence calculation units (FASTA digestion, residue mapping and MS1 XIC)
pyteomics==5.0.1
psims>=1.3,<2
numpy>=1.26,<3

# Batch 3 normalization helper. GlycReSoft should use its own supported environment.
glypy>=1.0.13,<2

# GlycReSoft is intentionally not pinned into the Python 3.12 API environment.
# Configure GLYCRESOFT_BIN to its separately validated CLI environment.
