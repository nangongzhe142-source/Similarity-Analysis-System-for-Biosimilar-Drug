# BioCompare CTD表征项目目录前端交付

## 本期范围

本期依据《V0.1生物类似药药学比对研究质量属性、检测方法及相似性评价原则汇总表.xlsx》中的“表征项目”和“CTD章节”搭建静态前端框架，不新增或修改底层运算引擎。

## 数据结构

- 共60个页面项目：59个具体项目及1个翻译后修饰总入口。
- 结构确证32项：一级结构、翻译后修饰、高级结构、糖基化。
- 理化特性12项：基本理化性质、纯度与分子尺寸异质性、电荷异质性。
- 生物学功能11项：靶标结合与作用机制、Fc受体及补体结合、效应功能。
- 杂质5项：产品相关杂质、工艺相关杂质。
- 6项现有模块标记为“已接入”；其余54项标记为“引擎待接入”。

CTD显示标签已经去除 `3.2.S.3` 等编号和页码，只保留章节文本。MS/MS序列确认覆盖率按确认方案继承“结构确证 / 一级结构”；其他工艺相关杂质按语义归入工艺相关杂质并显示源数据核对说明。

## 页面交互

- 一级总览继续默认收起，保持原有“查看全部比对项目总览”交互。
- 展开后显示飞书风格的“侧边分类导航 + 内容区”。
- 支持按四大类筛选。
- 支持按“全部 / 已接入 / 待接入”筛选。
- 支持项目名称、编号、分类和CTD章节全文搜索。
- 项目卡片显示项目编号、主名称、CTD小角标、接入状态和数据备注。
- 已接入项目可跳转原有专项页面；待接入项目按钮禁用，不触发任何后端调用。
- 完整目录下方保留原6项可运行模块的展开明细，原任务状态和运行逻辑不变。

## 技术边界

新增静态目录存放在 `lib/characterization-catalog.ts`，没有加入原 `projectModules` 运行数组。因此批量调度、材料路由、任务状态和报告汇总仍然只处理原6个已接入模块，不会误调度54个页面占位项目。

## 验证结果

- TypeScript：`tsc --noEmit`通过。
- Next.js 16.3.0生产构建通过，`/project`静态页面生成成功。
- 数据核验：60个唯一ID、60个唯一编号；结构确证32、理化特性12、生物学功能11、杂质5。
- 本地访问：`http://127.0.0.1:3000/project` 返回HTTP 200并包含新目录标题。

