# P21 — Next.js API 代理（2026-08-22）

**状态**：Done。  
**检查**：`npm run check` exit 0。

浏览器默认请求同源 `/api/analysis/…`，由 Next.js rewrite 转到 `http://127.0.0.1:8765`。可用 `ANALYSIS_SERVICE_URL` 改转发目标；`NEXT_PUBLIC_ANALYSIS_SERVICE_URL` 仍可让浏览器直连（调试用）。

分析服务本身仍监听 8765。离线提示改为：先启动 uvicorn，网页不直连 8765。

`scripts/verify_analysis_contract.mjs` 在 Windows 上给 Python 设 `PYTHONIOENCODING=utf-8`，避免 stdout 把 Schema 文档里的 `—` 读成替换字符而误报 stale。未改契约字段。

---

## 修改文件

| 文件 | 内容 |
|---|---|
| `next.config.ts` | `/api/analysis/:path*` rewrite |
| `src/lib/analysis-service-client.ts` | 默认基址 `/api/analysis` |
| `src/i18n/messages.ts` | 离线提示 |
| `analysis-service/README.md` | 不再写「浏览器直连」 |
| `scripts/verify_analysis_contract.mjs` | UTF-8 stdout |
