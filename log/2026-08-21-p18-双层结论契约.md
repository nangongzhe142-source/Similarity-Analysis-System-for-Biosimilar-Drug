# P18 — 双层结论契约（2026-08-21）

**状态**：Done。  
**硬边界**：法规 `verdict` 在图片路径上仍为 `REVIEW`。图像层结论只描述两张曲线是否相像。

---

## 契约

新增：

- `ThresholdKind`：`algorithmQualityGate` / `similarityBoundary`
- `ImageComparisonOutcome`：`CONSISTENT` / `INCONCLUSIVE` / `DIFFERENCE_OBSERVED` / `NOT_APPLICABLE`
- `AnalysisImageComparison`（`extractedFeatures.imageComparison`）

驱动指标固定为 `correlation`。`ssim` 与 `dtwDistance` 记入 `nonDiscriminatingMetrics`。阈值种类一律 `algorithmQualityGate`。缺可比较曲线 → `NOT_APPLICABLE`。

生成顺序：Python 模型 → `export_contract_schema.py write` → TypeScript → `verify_analysis_contract.mjs`。不手改 schema。

---

## 修改文件

| 文件 | 内容 |
|---|---|
| `analysis-service/app/models/analysis_contract.py` | 上述类型 |
| `src/types/analysis-contract.ts` | 同上 |
| `analysis-service/contracts/*` | 导出 |
| `analysis-service/app/analysis/image_fallback/pipeline.py` | `build_image_comparison` |
| `analysis-service/tests/test_image_fallback.py` | 条带与 thresholdKind 锁定 |

`node scripts/verify_analysis_contract.mjs`：当时 40 TS interfaces / 18 models，failures 0。P20 之后接口数已增加，以当时记录为准。
