# P12 — 专业结果可视化

**日期**：2026-08-21  
**状态**：Done  
**约束**：D9 前端交互 SVG + 后端 matplotlib PNG；每种图有实测或明确标注的 fixture；溯源含哈希/版本/参数/来源/局限性。不新建项目。

---

## 图类型与数据来源

| 图 | 数据 | PNG 产物 |
|---|---|---|
| 完整质量镜像谱 | `deconvolvedMassesDa`（intact-mass 合成演示 66398 / 66560 Da） | `mirror-plot.png` |
| LC-MS 色谱叠加 | peptide-map 合成 TIC（末峰 +0.40 min，标明 fixture）；图片降级为归一化列坐标且**不标 RT** | `overlay-plot.png` |
| 序列覆盖图 | MS1 / MS/MS `uncoveredRegions` + `sequenceLength` | `sequence-coverage-plot.png` |
| MS/MS 碎片离子 | Comet/注释 `fragmentIons`（b 上 / y 下） | `fragment-ion-plot.png` |
| 峰匹配表 | 匹配肽段 + Δppm；CSV `peak_table` | `candidate-peak-table.csv` / `candidate-psm-table.csv` |

## 前端

`MethodAnalysisPanel` 成功任务改为渲染 `AnalysisResultView`：交互 SVG、V2 规则、溯源面板、JSON 与产物下载。现有 MethodContentPanel / MethodLiveDemo / MethodToolPanel 未改行为。

## 后端

- `app/analysis/plots.py` — 四种 PNG
- `app/analysis/peptide_map/` + `PeptideMapAdapter`（合成 TIC，verdict 一律 REVIEW）
- `GET /v1/jobs/{id}/artifacts/{file_name}`（禁止路径穿越）
- 各 pipeline 填写 `provenance.inputHashes`

## 检查

| 命令 | 退出码 |
|---|---|
| `pytest tests/`（默认，不含 integration） | 0（69 passed, 7 deselected） |
| `npm run check` | 0 |

> **检查点 4**：P11–P12 完成，停下等确认。未开始 P13。
