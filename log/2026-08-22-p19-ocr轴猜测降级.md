# P19 — OCR 轴猜测不得当作校准（2026-08-22）

**状态**：Done。  
**边界**：OCR 读出的数字只进 `evidence.parameters.ocrNumericTokens`。`ocrCalibrationApplied` 恒为 `false`。没有操作员两点锚点时，`calibration.reliable` 仍为 false，分析降级为仅形状比对（无 Da / m·z⁻¹ / RT）。

P10 已实测：image1 的 OCR 把轴刻度读成 147605…148865，而峰标注是 148059…148546。因此本步不把 OCR 点数送进 `build_axis_calibration`。操作员两点校准仍是唯一可靠通道。

---

## 修改文件

| 文件 | 内容 |
|---|---|
| `analysis-service/app/analysis/image_fallback/calibration.py` | `ocr_numeric_tokens`；`ocr_guess_is_not_calibration` |
| `analysis-service/app/analysis/image_fallback/pipeline.py` | 记录 tokens，永不应用 |
| `analysis-service/tests/test_image_fallback.py` | 无操作员锚点时不得出现物理质量 |
