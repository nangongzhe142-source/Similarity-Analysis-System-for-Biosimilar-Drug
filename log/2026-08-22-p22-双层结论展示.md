# P22 — 前端双层结论展示（2026-08-22）

**状态**：Done。  
**检查**：`npm run check` exit 0。

法规 `verdict` 与图像层 `imageComparison` 分两栏。图像层「形状一致」用灰底，不用法规 PASS 的绿色。`qualityGates` 标明 `algorithmQualityGate`。V2 Sheet3 判定方法 / 数值边界 / 最终程序规则按 `itemId` 展示。图像峰配对表用归一化列坐标，不把未校准像素写成 Da。

有 `colourRoles` 时，叠加图从 `normalizedTraces.reference` / `.candidate` 取线；否则按红蓝与角色表回退。

---

## 修改文件

| 文件 | 内容 |
|---|---|
| `src/components/analysis/AnalysisResultView.tsx` | 双层结论、峰表、V2 条件、质量门 |
| `src/i18n/messages.ts` | 上述文案 |
| `scripts/verify_primary_analysis.mjs` | 机械检查上述字段仍在结果视图中 |
