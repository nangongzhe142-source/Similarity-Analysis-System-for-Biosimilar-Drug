# 综合相似性判别 — 实施计划

> 范围：在 `生物类似药相似性分析系统` 新增独立演示页 `/comprehensive-analysis`。
> 性质：交互式总体证据汇总演示。不声称监管认定，不输出「该产品是生物类似药」。
> 约束：不修改 `src/data/characterization-items.ts`；不改变现有单项目分析服务、上传流程与 `AnalysisResultView` 语义；不新增后端与持久化；每次只改一个文件。

## 已核实现状（实施前只读）

- 技术栈：Next.js 16.3.0 App Router、React 19.2.8、TypeScript 5.9.3 strict、Tailwind CSS 4.3.3。
- 国际化：`src/i18n/LanguageProvider.tsx` + `src/i18n/messages.ts`（`zh` / `en` 成对）。
- 导航：`src/components/layout/SiteHeader.tsx`（总览 / 特性鉴定 / 法规框架）。
- 数据：`characterizationItems` 共 61 项、9 个补充项；`categories` 共 8 类，数量为 11 / 15 / 7 / 2 / 7 / 3 / 11 / 5。
- 校验：无 Jest/Vitest；现有方式为 `scripts/verify_*.mjs`，由 `npm run check` 串联。
- 页面样例：`src/app/regulatory/page.tsx` → 客户端 View 组件。

## 冻结实现约定（避免科学性误导）

1. 适用性与演示状态并存：`isApplicable === false` 或 `demoStatus === "not-applicable"` 均视为不适用；不适用项不进总体分母，但非补充项缺少不适用原因时视为未完成。
2. 参与汇总：非补充 ∧ 适用。补充项可展示，永不参与总体结论。
3. 适用项完成条件：已选择支持/不支持/证据不足，且候选描述、参照描述、比对说明在 zh 或 en 任一语种有非空白文本。完整度与结论不随界面语言切换变化。
4. 保守汇总优先级：任一参与项「不支持相似」→ 不支持相似性证据；否则若无参与项、或存在证据不足/未选状态/必要文本缺失/不适用缺原因 → 证据不足；仅当全部参与项均为「支持相似」且无上述缺口 → 支持相似性证据。
5. 自由文本不驱动判定：禁止关键词分析；演示状态只来自选择器。演示图形由 `itemId` + 演示状态的确定性函数生成，并标注「演示流程 / 尚未接入真实计算 / 不参与监管判断」。
6. 演示数据独立模块，产品名明确为 illustrative/demo，不复用审评案例数值，不写入 `AnalysisResult`。

## 步骤

### 步骤 1：创建本实施计划并冻结实现约定

Done

本文件列出只读核对结果、科学性边界与有序文件级步骤，作为后续唯一实施顺序。
未改任何业务代码；`characterization-items.ts` 保持只读，现有分析页语义不变。

### 步骤 2：新增综合判别类型与状态常量

文件：`src/types/comprehensive-analysis.ts`

Done

集中定义演示状态、总体结论、完成缺口与会话/汇总结构，全部为字面量常量而非魔术字符串。
未引入 `any`，也未改动 `models.ts` 或现有分析契约类型。

### 步骤 3：实现无副作用汇总纯函数

文件：`src/lib/comprehensive-analysis/summarize.ts`

Done

实现保守优先级汇总：不支持优先，其次证据不足/未完成，全部支持才给出支持相似性证据。
函数无 I/O、无随机数、无加权；补充项与不适用项按约定排除在总体分母之外。

### 步骤 4：实现确定性演示流程派生函数

文件：`src/lib/comprehensive-analysis/demo-process.ts`

Done

七步过程视图与示意图形均由 `itemId` 与演示状态确定性派生，相同输入得到相同输出。
图形不把自由文本当作检测值，也不根据关键词改写项目结论。

### 步骤 5：新增独立演示数据模块

文件：`src/data/comprehensive-analysis-demo.ts`

Done

演示会话由 `characterizationItems` 生成并覆盖支持/不支持/证据不足/不适用四态，产品名标明 illustrative。
补充项可展示但不作为总体结论来源；清空与恢复均返回深拷贝，避免污染真实分析数据。

### 步骤 6：扩展中英文 UI 文案

文件：`src/i18n/messages.ts`

Done

导航增加「综合判别 / Integrated Assessment」，并补齐结论、表单、总览、过程步骤与免责声明的中英文。
未改动既有页面文案键，避免影响现有界面语义。

### 步骤 7：状态字形组件

文件：`src/components/comprehensive-analysis/AssessmentStatusGlyph.tsx`

Done

支持、不支持、证据不足、不适用与未选择分别使用圆、八角、方、菱与虚线圆，并同时给出文本标签。
颜色只作为辅助，不作为唯一状态通道。

### 步骤 8：基础信息录入表单

文件：`src/components/comprehensive-analysis/ProductPairForm.tsx`

Done

基础信息六项均有 label，编辑写入当前语言字段；提供清空并重新录入与恢复演示数据操作。
状态仍由父组件持有，本组件不引入后端或持久化。

### 步骤 9：顶部总体证据结论区

文件：`src/components/comprehensive-analysis/OverallEvidencePanel.tsx`

Done

结论区位于明细之前，含大号状态字形与文本、产品对、依据、计数、完整度、关键项目锚点与免责声明。
关键项目使用业务 ID 作为链接目标，不把颜色当作唯一状态表达。

### 步骤 10：直观比对总览

文件：`src/components/comprehensive-analysis/ComparisonOverview.tsx`

Done

总览用 CSS 条形与大类卡片展示状态计数、完成占比及候选/参照并列，不引入图表依赖。
点击大类卡片滚动并聚焦到对应明细锚点 `#category-{key}`。

### 步骤 11：项目比对过程面板

文件：`src/components/comprehensive-analysis/ItemComparisonProcess.tsx`

Done

按固定七步展示输入、演示预处理、并列示意、文本层差异、框架判定原则、项目结论与来源限制。
未接入真实计算的步骤均标注演示流程，示意图形不冒充实测图谱。

### 步骤 12：检测项目卡片

文件：`src/components/comprehensive-analysis/ItemAssessmentCard.tsx`

Done

每个项目卡片展示名称、补充标记、适用性、演示状态、完整度与摘要，列表 key 使用 `item.id`。
展开过程区使用 `aria-expanded` 与 `aria-controls`；表单控件均有 label。

### 步骤 13：质量属性大类区块

文件：`src/components/comprehensive-analysis/CategoryAssessmentSection.tsx`

Done

按现有 `categories` 分组渲染，折叠按钮提供 `aria-expanded` 与 `aria-controls`。
存在「不支持相似」或「证据不足」的大类通过 `defaultExpanded` 默认展开。

### 步骤 14：页面主视图

文件：`src/components/comprehensive-analysis/ComprehensiveAnalysisView.tsx`

Done

主视图预载演示会话，按结论→总览→表单→八大类明细排列，并用 `useMemo` 计算汇总。
清空与恢复通过深拷贝替换会话并重置折叠状态；状态仅存在于前端内存。

### 步骤 15：新增路由页面

文件：`src/app/comprehensive-analysis/page.tsx`

Done

新增 App Router 页面 `/comprehensive-analysis`，挂载综合判别主视图。
未改动既有路由与单项分析页。

### 步骤 16：导航入口

文件：`src/components/layout/SiteHeader.tsx`

Done

导航增加中文「综合判别」与英文「Integrated Assessment」，当前路径高亮。
页头与导航允许换行，避免移动端入口被挤出可视区。

### 步骤 17：汇总规则自动化校验脚本

文件：`scripts/verify_comprehensive_analysis.mjs`

Done

校验脚本直接调用汇总纯函数，覆盖不支持、证据不足、未完成、全部支持、全部不适用、补充项、缺原因、演示稳定性、自由文本与语言无关性。
10 个场景已在本地运行通过，失败时以非零退出码终止。

### 步骤 18：接入项目检查流程

文件：`package.json`

Done

新增 `verify:comprehensive-analysis`，并将其串入 `npm run check` 末尾。
未关闭既有校验，也未忽略错误。

### 步骤 19：运行既有检查命令

命令：`npm run typecheck`、`npm run lint`、`npm run check`、`npm run build`

Done

typecheck、lint、check（含新增 10 场景校验）与 build 均以退出码 0 通过；`/comprehensive-analysis` 已进入静态路由。
检查过程中清除了未使用类型导入，并将总体结论大号文案改为「支持/不支持相似性证据」，未忽略任何失败项。

---

## FDA 风格视觉升级

> 目标：权威监管网站 + 现代科研数据平台的视觉品质。参考 USWDS 的层级、对比度与页头结构，不复制 FDA 标志、名称、政府横幅或官方隶属关系。
> 约束：不改汇总算法、上传/分析语义、演示数据逻辑；不引入外部字体/图片/脚本；每次只改一个文件。

### 视觉令牌（冻结）

- 海军：`#0b1a2e` / `#112f4e`；品牌蓝：`#0b5cab`；青蓝：`#0e7490`；强调红：`#b42318`。
- 画布：`#eef3f8`；纸面：`#ffffff`；正文：`#1a232d`；次级文字：`#3a4654`（对比度目标 ≥ 4.5:1）。
- 圆角 4px / 8px；阴影克制；过渡 180ms；`prefers-reduced-motion` 下过渡为 0。

### 步骤 V1：写入本升级章节并冻结令牌

Done

在计划中记录只读审计、令牌、复用链与单文件改造顺序，作为视觉升级唯一执行清单。
未改业务代码；相似性汇总规则与分析服务语义保持不变。

### 步骤 V2：补齐视觉升级所需中英文案

文件：`src/i18n/messages.ts`

Done

补齐跳转主内容、移动菜单、非官方声明、Hero 双操作与综合判别入口的中英文，未改业务语义文案。
未删除既有键；综合判别免责声明原文保持不变。

### 步骤 V3：建立全局 CSS 设计令牌

文件：`src/app/globals.css`

Done

以 `@theme` 定义海军/品牌蓝/青蓝/信号红与画布、阴影、圆角；加入 Hero 网格、焦点环与减动效。
未引入外部字体或脚本；修正了 `.tap-target` 选择器写法。

### 步骤 V4：根布局接入跳转主内容与画布

文件：`src/app/layout.tsx`

Done

`main` 设置 `id="main-content"` 作为跳转目标，body 使用画布网格背景与 `text-ink`。
未改 metadata 与 LanguageProvider 结构；跳转链接放在客户端页头以便双语。

### 步骤 V5：语言切换器适配深色顶栏

文件：`src/components/LanguageSwitcher.tsx`

Done

增加 `light` / `dark` 变体与 44px 触控目标，语言切换仍写入现有 localStorage 机制。
未改 Locale 类型与默认语言。

### 步骤 V6：双层权威页头与移动导航

文件：`src/components/layout/SiteHeader.tsx`

Done

海军品牌条 + 品牌蓝底边主导航；移动端为独立抽屉而非仅换行；含跳到主要内容与 Escape 关闭。
分类菜单展示序号与数量；声明条明确本站非政府官方网站，无 FDA 标志。

### 步骤 V7：页脚对比度与层级

文件：`src/components/layout/SiteFooter.tsx`

Done

页脚改为海军底与品牌蓝顶边，来源与免责文字使用高对比浅色。
保留法规来源字段与既有免责声明语义，并并列非官方网站说明。

### 步骤 V8：面包屑

文件：`src/components/Breadcrumb.tsx`

Done

链接使用品牌蓝与可见焦点，当前页文字改为 `text-ink` 以满足对比度。
路由 `href` 与条目结构未改。

### 步骤 V9：大类线性图标

文件：`src/components/CategoryMark.tsx`

Done

8 个大类各有序号与内联线性 SVG，图形随 `currentColor` 着色，不请求外部资源。
图标仅为视觉识别，不新增业务数据。

### 步骤 V10：分类卡片

文件：`src/components/CategoryCard.tsx`

Done

卡片加入序号图标、品牌蓝左边框与始终可见的详情入口，悬停仅加强阴影。
链接仍指向 `/category/{key}`，项目数量来自既有 props。

### 步骤 V11：检测项目卡片

文件：`src/components/ItemCard.tsx`

Done

项目卡改用纸面层级与 `line-clamp-2` 目的摘要，参考案例标签改青蓝高对比。
仍以 `item.id` 链接详情，数据字段未改。

### 步骤 V12：适用性徽章

文件：`src/components/ApplicabilityBadge.tsx`

Done

徽章改为品牌蓝边与海军文字，完整文本仍通过 `title` 与可见文字提供。
未改 `applicability` 数据源。

### 步骤 V13：补充项标签

文件：`src/components/SupplementaryTag.tsx`

Done

补充项同时使用菱形色块与文字，边框与海军文字保证对比度。
标签文案仍来自 `common.supplementaryTag`。

### 步骤 V14：评价字段块

文件：`src/components/FieldBlock.tsx`

Done

判定关键字段用品牌蓝左边框强调，正文改为 `text-ink`。
仍只渲染传入的 `LocalizedText`，空值占位逻辑不变。

### 步骤 V15：首页 Hero、指标与双入口

文件：`src/components/views/HomeView.tsx`

Done

Hero 使用海军到品牌蓝渐变与网格纹理，指标为高对比数字卡，并提供综合判别与大类锚点主操作。
免责声明改为次级纸面区块；未编造新的统计数字。

### 步骤 V16：大类列表页

文件：`src/components/views/CategoryView.tsx`

Done

标题区改为纸面卡 + 大类图标与序号；补充项说明改为海军文字以满足对比度。
列表仍由 `getItemsByCategory` 驱动。

### 步骤 V17：项目详情页外壳

文件：`src/components/views/ItemDetailView.tsx`

Done

详情页标题区改为纸面卡与品牌左边框；分区标题与前后项导航使用海军/品牌蓝。
MethodSelector 与参考案例数据流未改。

### 步骤 V18：法规框架页

文件：`src/components/views/RegulatoryView.tsx`

Done

法规页标题区使用青蓝左边框以区别于特性鉴定，来源文字提高对比度。
两张表格的数据源仍为 `regulatoryFramework`。

### 步骤 V19：法规表格

文件：`src/components/RegulatoryTable.tsx`

Done

表头改为海军底白字；关系徽章同时使用形状与文字；窄屏保留横向滚动。
行 `id` 与单元格数据映射未改。

### 步骤 V20：单项分析占位槽

文件：`src/components/SimilarityAnalysisPlaceholder.tsx`

Done

占位槽对齐画布色与虚线边框，禁用按钮仍不可用且文案不变。
未开放上传，也未改 `analysisPlaceholder` 槽位过滤。

### 步骤 V21：综合判别页外壳

文件：`src/components/comprehensive-analysis/ComprehensiveAnalysisView.tsx`

Done

标题区改为纸面卡与 44px 主操作按钮，清空/恢复仍调用既有会话工厂。
`useMemo` 汇总与 `sessionEpoch` 逻辑未改。

### 步骤 V22：总体证据面板

文件：`src/components/comprehensive-analysis/OverallEvidencePanel.tsx`

Done

计数区改为指标卡，链接与免责声明容器改用品牌色边框。
`copy.disclaimer` 与 `copy.demoUseBanner` 原文及汇总字段未改。

### 步骤 V23：比对总览

文件：`src/components/comprehensive-analysis/ComparisonOverview.tsx`

Done

计量条与大类入口改为纸面卡、品牌色条与 `CategoryMark`，完成度条使用品牌蓝。
计数仍读取 `inventorySupportsCount` 等 inventory 字段，跳转与 `role="meter"` 未改。

### 步骤 V24：基础信息表单

文件：`src/components/comprehensive-analysis/ProductPairForm.tsx`

Done

表单容器改为纸面卡，输入框与 44px 按钮使用品牌色与 `border-line`。
`updateLocalizedField` 与各字段 `onProductPairChange` 写入路径未改。

### 步骤 V25：大类判别区块

文件：`src/components/comprehensive-analysis/CategoryAssessmentSection.tsx`

Done

区块改为纸面卡与品牌左边框，折叠按钮使用 44px 触控目标，标题旁加入 `CategoryMark`。
`useState(defaultExpanded)`、`createFallbackItemEntry` 与 `aria-expanded` 逻辑未改。

### 步骤 V26：项目判别卡片

文件：`src/components/comprehensive-analysis/ItemAssessmentCard.tsx`

Done

卡片改为纸面卡，表单控件与单选标签使用 44px 触控目标与 `border-line`。
适用性、演示状态与文本字段的 `onEntryChange` 语义未改。

### 步骤 V27：状态字形配色对齐令牌

文件：`src/components/comprehensive-analysis/AssessmentStatusGlyph.tsx`

Done

配色改为品牌蓝、信号红、海军蓝与墨色次级令牌。
圆形对号、八边形叉号、方块问号、菱形横线与虚线圆形状及旁注文字均保留。

### 步骤 V28：运行自动化检查

命令：`npm run typecheck`、`npm run lint`、`npm run check`、`npm run build`

Done

`typecheck`、`lint`、`check`、`build` 均以退出码 0 通过；页头改为随 `pathname` 重挂载以关闭菜单，去掉 effect 内同步 `setState`。
localhost 首页、综合判别、法规与大类页均返回 200，并含 skip-link 与 `main#main-content`；综合判别 10 场景校验仍通过。

---

## Dify Chatflow AI 助手

> 范围：在本站用本机 Dify 1.14.1 创建可追溯 Chatflow，经本站服务端代理嵌入右下角悬浮窗口。
> 性质：解释系统资料与已有分析结果。不构成生物类似药认定或监管建议。
> 约束：不改分析服务、上传流程、`AnalysisResult` 契约与 `characterization-items.ts`；不把 Dify API Key 写入前端或 `NEXT_PUBLIC_*`；不直接改 Dify PostgreSQL；每次只改一个文件。

### 已核实现状（实施前只读，2026-08-27）

- 目标项目：`d:\生物类似药判别系统\生物类似药相似性分析系统`。
- 技术栈：Next.js 16.3.0 App Router、React 19.2.8、TypeScript 5.9.3 strict、Tailwind CSS 4.3.3；无 Jest/Vitest，校验为 `scripts/verify_*.mjs` + `npm run check`。
- 网页：`src/app/layout.tsx` 挂 `LanguageProvider` + `SiteHeader` + `main#main-content` + `SiteFooter`。路由为 `/`、`/category/[key]`、`/item/[id]`、`/regulatory`、`/comprehensive-analysis`。站点无任何 assistant/chat/dify 代码；无 `src/app/api/**` 路由（分析走 `next.config.ts` rewrite `/api/analysis` → `http://127.0.0.1:8765`）。
- 页面上下文缺口：`MethodSelector` 的 `selectedMethodId` 与 `MethodAnalysisPanel` 的 `snapshot` 均为组件本地 state，布局级悬浮窗当前读不到方法与分析结果。
- 分析结果类型：`src/types/analysis-contract.ts` 的 `AnalysisResult` / `AnalysisJobSnapshot`（`schemaVersion` 1.0.0，`ruleSetVersion` `v2-sheet3-8bd6b18f`）。
- 国际化：`src/i18n/LanguageProvider.tsx`，locale `zh`/`en`，默认 `zh`，localStorage 键 `biosimilar-framework.locale`。
- Dify：Compose 目录 `D:\LECHENG POLICY AI AGENT\docker`，镜像 `langgenius/dify-api:1.14.1` 与 `langgenius/dify-web:1.14.1`，容器 healthy。控制台 `http://localhost/`（nginx 80/443）；Service API `http://localhost/v1`。`GET /console/api/setup` → `step=finished`（2026-05-15）。`GET /console/api/system-features` → `app_dsl_version=0.6.0`，邮箱密码登录开启，禁止注册、禁止新建工作区。无鉴权时 `GET /console/api/apps` 与 `POST /v1/chat-messages` 均 401。未取得登录信息，故不能列举工作区、已有应用、知识库或 API Key。
- Chat API（官方文档）：`POST {api_base}/chat-messages`，必填 `inputs`、`query`、`user`；`response_mode` 为 `streaming` 或 `blocking`；`conversation_id` 空则新会话。鉴权 `Authorization: Bearer {api_key}`。
- 知识资料实存（`d:\生物类似药判别系统\生物类似药评价指导原则`）：
  - `V2-生物类似药药学比对研究质量属性、检测方法及相似性评价原则汇总表.xlsx`（40273 B，SHA-256 `8bd6b18f08d3b9a3b99c61cdb8eeff24ceef75a54a43b9f7d14d1709b703051f`，与 `similarity-schemes.ts` 一致）
  - `《生物类似药相似性评价和适应症外推技术指导原则》.pdf`
  - `生物类似药研发与评价技术指导原则（试行）.doc`
  - `生物类似药首次申报临床试验药学资料撰写指导原则.pdf`
  - `生物类似药药学评价比较.docx`
  - `判断相似性的标准.docx`
  - `V3.7-生物类似药课题中国部分-20260629韩欣烘.docx`（课题内部稿）
  - `20260817组会会议记录.docx`（会议记录）
- 站点已结构化资料：`method-content.ts`（一级结构 33 条方法原理）、`similarity-schemes.ts`（Sheet3 共 11 项，其中 7 项规则完整、4 项 `RULE_NOT_DEFINED`）、`regulatory-framework.ts`、`characterization-items.ts`、GP2015 参考案例（含 `source`/`verification`；含 `illustrative` 示意案例）、`README.md`、`analysis-service/README.md`。
- `.gitignore` 已忽略 `.env*`。环境变量不得提交。

### 冻结实现约定

1. 链路：浏览器 → `POST /api/assistant/chat`（本站 Route Handler）→ `http://127.0.0.1/v1/chat-messages`。禁止浏览器携带 Dify Key，禁止客户端指定上游 URL。
2. 服务端环境变量：`DIFY_API_BASE_URL`（默认 `http://127.0.0.1/v1`）、`DIFY_APP_API_KEY`、`DIFY_TIMEOUT_MS`（默认 60000）。不得使用 `NEXT_PUBLIC_*` 存放密钥。
3. Chatflow 模式：`advanced-chat`。开始节点输入变量名冻结为：`pageUrl`、`pageTitle`、`locale`、`categoryKey`、`itemId`、`itemName`、`methodId`、`methodName`、`analysisStatus`、`analysisResult`、`analysisProvenance`。DSL `version` 必须与控制台返回的 `0.6.0` 对齐，导入后用节点变量选择器核对，不手写未证实的 Jinja 语法。
4. `analysisResult` 只传白名单字段（见 `src/lib/assistant/analysis-result-whitelist.ts`），截断超长数组与产物路径；缺失时传空字符串，助手不得推测。
5. 方法学/规则/法规优先知识库；结果解释只使用传入的结构化结果与溯源；越权判定、看图猜峰、忽略系统规则一律拒绝。
6. 用户标识：符合 UUID 的匿名稳定 id（cookie 或 localStorage），不传邮箱/姓名。
7. 渲染：助手回复按纯文本显示，不执行 HTML。免责声明固定为：「AI 助手仅用于解释系统资料和分析结果，不构成生物类似药认定或监管建议。」
8. 知识导入：用户确认「全部导入」指导原则目录全部文件 + 站点结构化资料。Excel 转为带表名/行号/字段名/源文件的 Markdown；案例保留监管机构、产品、文件、章节/页码与 `evidenceLevel`；示意案例、内部会议、课题内部稿必须标注资料性质。不导入 `node_modules`、构建产物、日志、质谱 fixture。
9. 不覆盖已有 Dify 应用：仅新建；导出 DSL 不得含密钥。

### 知识筛选（用户 2026-08-27 确认全部导入）

**导入：** V2 Excel 结构化导出；指导原则目录全部 PDF/DOC/DOCX（含会议记录与课题内部稿，文首标注资料性质）；`method-content` / `similarity-schemes` / `regulatory-framework` / 检测项目目录 / 方法分析状态；GP2015 案例（监管与示意分文件）；本站 README 与 `analysis-service/README.md`。

**不导入：** `node_modules`、构建产物、`log/`、质谱/图片 fixture。

### 步骤

### 步骤 D0：写入本实施计划并冻结约定

Done

本文件记录只读核查结果、Dify 1.14.1 访问地址、Chat API 约束、知识筛选待确认项与有序文件步骤。
未改业务代码，未登录 Dify，未创建应用或知识库。

### 步骤 D1：取得 Dify 登录与知识确认（阻塞）

Done

控制台登录成功。工作区仅 1 个：`nangong zhe's Workspace`（`54b4c2b2-ef2b-4d03-8894-e50589e7b478`，current=true）。已有应用 3 个（知识图谱 workflow、Regulation Policy AI Agent advanced-chat、乐城政策智能助手 chat），均非本助手，按确认新建。
知识按「全部导入」执行；系统提示词已由用户提供；默认 LLM `deepseek-v4-flash`（`langgenius/deepseek/deepseek`），嵌入 `text-embedding-v4`（通义，与现有高质量库一致）。凭证不写入仓库。

### 步骤 D2：导出知识用结构化 Markdown 转换脚本

文件：`scripts/export_assistant_knowledge.py`

Done

脚本从 V2 Excel、方法学/规则 sidecar、法规框架、项目目录、案例与 README 生成带溯源字段的 Markdown，并抽取指导原则目录全部 PDF/DOC/DOCX。
不读取 node_modules、构建产物、日志或质谱 fixture；Excel SHA-256 与 sidecar 记录一致才继续。

### 步骤 D3：运行知识导出并核对溯源字段

命令：`python scripts/export_assistant_knowledge.py`

Done

退出码 0；写出 19 个文件到 `knowledge/assistant/`。Excel 三张表含表名/单元格坐标；方法学含 methodId/itemId；监管案例与示意案例分文件并带 evidenceLevel。

### 步骤 D4：分析上下文白名单纯函数

文件：`src/lib/assistant/analysis-result-whitelist.ts`

Done

从 `AnalysisResult` 抽出允许传给 Dify 的字段，数组与总长分别限制为 12 与 12000 字符；缺 `verdict` 返回 `null`，不编造结论。
未修改 `analysis-contract.ts`。

### 步骤 D5：页面助手上下文类型与发布器

文件：`src/lib/assistant/page-context.ts`

Done

定义 `AssistantPageContext` 与 `publishAssistantPageContext` / `useAssistantPageContext`，供详情页、方法选择与分析面板写入、悬浮窗读取。
不含 API Key。

### 步骤 D6：Dify 代理请求校验

文件：`src/lib/assistant/chat-request.ts`

Done

校验 query/inputs 长度，`user` 必须为 UUID，拒绝 `endpoint`/`difyUrl` 等客户端上游字段；本地护栏检测越权、整品认定与看图猜峰。
不发网络请求。

### 步骤 D7：服务端 Dify 客户端

文件：`src/lib/assistant/dify-client.ts`

Done

仅读取 `DIFY_API_BASE_URL` / `DIFY_APP_API_KEY` / `DIFY_TIMEOUT_MS`，转发 `POST /chat-messages`（streaming）。
日志不记录 Key；上游主机不可由请求覆盖。

### 步骤 D8：助手 API 路由

文件：`src/app/api/assistant/chat/route.ts`

Done

`POST` 校验体后转发 Dify；不可达返回 `DIFY_UNAVAILABLE`；未配置返回 `DIFY_UNCONFIGURED`；流式透传 SSE。
按 user 限流 20 次/分钟。

### 步骤 D9：助手健康检查路由

文件：`src/app/api/assistant/health/route.ts`

Done

仅返回 `configured` 与 `reachable` 两个布尔值。
不暴露 Key、内部 URL 或其它配置细节。

### 步骤 D10：中英文助手文案

文件：`src/i18n/messages.ts`

Done

增加开闭按钮、新建会话、加载/超时/网络/不可用/未配置、占位提示与免责声明的 `zh`/`en`。
中文免责声明与冻结原文一致；未改既有键语义。

### 步骤 D11：悬浮聊天面板

文件：`src/components/assistant/AssistantPanel.tsx`

Done

消息列表、输入、流式展示、新建会话、防重复提交；回复按纯文本渲染。
桌面与移动共用同一面板。

### 步骤 D12：右下角悬浮按钮与壳

文件：`src/components/assistant/AssistantWidget.tsx`

Done

固定右下角开闭；读取页面上下文与 locale；请求 `/api/assistant/chat`。
匿名 user UUID 存 localStorage；不把 Key 放进客户端。

### 步骤 D13：根布局挂载助手

文件：`src/app/layout.tsx`

Done

在 `LanguageProvider` 内挂载 `AssistantWidget`。
未改 metadata 与 `main#main-content` 结构。

### 步骤 D14：详情页发布项目上下文

文件：`src/components/views/ItemDetailView.tsx`

Done

进入时发布 `categoryKey`、`itemId`、`itemName`，离开时清理。
未改评价字段与案例区。

### 步骤 D15：方法选择发布 methodId

文件：`src/components/MethodSelector.tsx`

Done

选中方法变化时发布 `methodId`/`methodName`；发布 effect 放在 `methods.length===0` 提前返回之前。
未改分析/演示/工具面板顺序。

### 步骤 D16：分析面板发布结果摘要

文件：`src/components/MethodAnalysisPanel.tsx`

Done

job 状态变化时发布 `analysisStatus` 与白名单后的 `analysisResult`/`analysisProvenance`。
未改上传与计算。

### 步骤 D17：Chatflow DSL（无密钥）

文件：`dify/biosimilar-assistant-chatflow.yml`

Done

`kind: app`，DSL `0.6.0`，`advanced-chat`；开始节点含 D0 冻结的 11 个变量；含校验、分类、知识检索、LLM、Answer 与越权短路。
不含 API Key。LLM 系统提示词为用户确认文本，变量语法为 `{{#节点id.变量#}}`。

### 步骤 D18：在实际 Dify 中创建知识库并导入筛选文档

操作：Dify Console 或官方 Knowledge API（需 D1 凭证）。记录真实 dataset id。不改 PostgreSQL。

Done

新建高质量知识库 `Biosimilar Similarity Assistant KB`，id `6637dbaf-2f0d-4b26-9fe2-3cf3713317c7`，嵌入 `text-embedding-v4` / `langgenius/tongyi/tongyi`。导入 `knowledge/assistant` 下 20 个 Markdown，以及指导原则目录 PDF/DOCX（`.doc` 被 Knowledge API 以 400 拒绝后，用 Word 另存为 `.docx` 再导入）；索引 27/27 `completed`。未改 PostgreSQL，未导入 `~$` 临时文件与 xlsx 原件（Excel 已按单元格导出为 Markdown）。

### 步骤 D19：导入 Chatflow DSL、绑定知识库、配置系统提示词并发布

操作：`POST /console/api/apps/import`（mode `yaml-content`）或控制台导入；用节点变量选择器绑定上下文与检索结果；创建应用 API Key 写入本机 `.env.local`（不入库）。导出更新后的 DSL 覆盖 `dify/biosimilar-assistant-chatflow.yml`（仍无密钥）。

Done

`POST /console/api/apps/imports`（`yaml-content`）新建应用 `生物类似药药学相似性助手`，id `6d42d9b2-4fda-4958-869a-ff1430ca2d66`，`advanced-chat`；三个既有应用 id 未变。草稿开始节点 11 个变量齐全，知识检索 `dataset_ids` 为上述知识库 id；`POST .../workflows/publish` 返回 success；应用 API Key 只写入本机 `.env.local`。`include_secret=false` 导出覆盖 DSL，无 `app-`/`dataset-` 明文密钥。

### 步骤 D20：助手校验脚本

文件：`scripts/verify_assistant.mjs`

Done

覆盖白名单截断、缺结果返回空、越权/整品认定/看图猜峰护栏、拒绝客户端上游 URL、源码与非缓存 `.next` 文本不含 API Key 字面值。
`npm run verify:assistant` 退出码 0。

### 步骤 D21：接入 `npm run check`

文件：`package.json`

Done

新增 `verify:assistant`，串在 `check` 末尾。
未关闭既有校验。

### 步骤 D22：环境变量说明

文件：`README.md`

Done

记录 `DIFY_API_BASE_URL`、`DIFY_APP_API_KEY`、`DIFY_TIMEOUT_MS`、免责声明与禁止 `NEXT_PUBLIC_*`。
未写入真实 Key。

### 步骤 D23：对真实 Chatflow 的 API 级验收

命令：在已配置 `.env.local` 下运行针对 `/v1/chat-messages` 与 `/api/assistant/chat` 的验收（知识内问题、无资料、越权拒绝、有/无分析结果、中英文、上游不可用）。记录真实来源字段是否返回。

Done

`POST /v1/chat-messages`：方法学问答 200 且 `retriever_resources` 非空；无资料走证据不足模板；越权/看图猜峰/整品认定均为固定拒绝话术；无分析结果走「尚未提供可解释的结构化分析结果」；英文问答 200 且有检索来源；关闭端口上游返回 URLError。
`GET /api/assistant/health` → `{configured:true,reachable:true}`；`POST /api/assistant/chat` 整品认定 200 SSE 含固定话术；客户端 `endpoint` 返回 `FORBIDDEN_FIELD`；首页 HTML 不含 API Key。

### 步骤 D24：运行既有检查与生产构建

命令：`npm run typecheck`、`npm run lint`、`npm run check`、`npm run build`

Done

四条命令均退出码 0。`next build` 生成 `/api/assistant/chat` 与 `/api/assistant/health` 动态路由。
抽查 `http://127.0.0.1:3000/`、`/comprehensive-analysis`、`/regulatory`、`/category/primary-structure`、`/item/intact-mass` 均 200 且含 `main#main-content`。

---

## 人工复核 Human Input（保持 Chatflow）

> 范围：把现 Chatflow 从短路拒绝改为「先完善回复，再内部 Human Input」；已批准复核写入独立知识库。
> 约束：不改 `characterization-items.ts`、分析契约、分析服务；不把 Dify Key 写入前端或 `NEXT_PUBLIC_*`；DSL 无密钥；不微调模型；复核不得改写当次数值/状态。
> 站点：继续 `POST /api/assistant/chat` → `/v1/chat-messages`。不改为 `/v1/workflows/run`。

### 已核实现状与最优解（2026-09-01）

- 现应用仍为 `advanced-chat`，DSL `0.6.0`，开始节点 11 个变量与 D0 冻结名一致。
- 现图含多条短路 Answer（空输入、注入、整品认定、看图猜峰、证据不足、无分析结果、越权），与「必须先给出完善回复」冲突。
- Dify 1.14.1 源码确认：Human Input 节点 type 为 `human-input`；交付为 `webapp` / `email`；动作句柄为各 `user_actions.id` 与超时 `__timeout`；Answer 可以接到下游（本机 fixture `test-answer-order.yml`）。
- `mode: workflow` 会改成 `/v1/workflows/run` 且没有 Chatflow Answer，无法在暂停前把完善回复发给现站点。因此保持 `advanced-chat`：LLM → Answer → Human Input。这不是用「聊天里填表」做复核入口；终端用户只看到完善回复，复核表单走 Email/控制台。
- 空输入：用户确认只追问困扰，不走 8 块、不进 Human Input。
- Human Review KB 在控制台新建后，把 Dataset UUID 与 Dataset API Key 填入应用环境变量；DSL 只保留变量名。

### 冻结实现约定

1. 模式保持 `advanced-chat`。开始变量名不变。temperature 0.1。免责声明固定原文。
2. 除空输入外，任何用户问题都先经 LLM 生成 8 块完善回复，由 Answer 发出，再暂停 Human Input。不设问题分类节点。
3. Human Input 仅内部药学人员。`email` 启用且 `webapp` 关闭；收件人由操作员在控制台实机填写。超时 3 天，超时不入库。
4. 已批准复核只写入 `Biosimilar Human Review KB`。禁止写入 `Biosimilar Similarity Assistant KB`。
5. 入库门槛全部满足才 HTTP `create-by-text`。不满足则内部 `{"action":"reject","reason":"..."}`，不撤回已发完善回复。
6. 检索优先级：Human Review 中 `status: approved` 且未过期 → 原库 → 当次 `analysisResult`。当次数值/状态不可被复核改写。
7. 空上下文字段在装配后写「空」，禁止推断。
8. 空输入：不走 8 块、不进 Human Input。由 `LLM·空输入追问` 先问候再理性追问，合计最多三句；可点出非空页面字段。语言：locale 为 en 用英文，否则中文。

### 步骤 H0：写入本段并冻结约定

Done

记录保持 Chatflow、先 Answer 再 Human Input、独立 Human Review KB、站点 API 不变。
未改业务代码，未登录 Dify 控制台。

### 步骤 H1：重写助手 DSL（无密钥）

文件：`dify/biosimilar-assistant-chatflow.yml`

Done

删除短路拒绝 Answer；空输入只追问；其余路径检索原库 + Human Review 候选、生成完善回复、Answer、Human Input、入库门槛与 HTTP。
环境变量只保留名称；密钥值为空。人工复核检索节点导入后须改绑到新库。

Dify 1.14.1 导入校验要求 Human Input 短文本为 `text_input`（开始节点仍为 `text-input`），投递渠道 `id` 为 UUID。

### 步骤 H1b：去掉分类并改为空输入问候追问

文件：`dify/biosimilar-assistant-chatflow.yml`

Done

删除问题分类节点；非空问题直接检索原库与人工复核库。空输入改走 `LLM·空输入追问` 再 `直接回复·空输入追问`，不进 Human Input。
站点仍为 Chatflow；Human Input 收件留待控制台实机填写。

### 步骤 H2：允许空问题到达 Dify

文件：`src/lib/assistant/chat-request.ts`

Done

空字符串 query 可以转发到 Dify 空输入追问；缺少 query 字段仍为 INVALID_BODY。
仍拒绝客户端上游字段，仍截断过长 query。

### 步骤 H3：前台允许空发送且不把暂停当失败

文件：`src/components/assistant/AssistantPanel.tsx`，随后 `AssistantWidget.tsx`

Done

发送按钮不再因草稿为空而禁用；SSE 忽略 Human Input 暂停事件，已发出的完善回复保留。
不渲染复核表单，不把 Key 放进客户端。

### 步骤 H4：助手校验覆盖新约定

文件：`scripts/verify_assistant.mjs`

Done

断言空 query 可转发、DSL 含 human-input 与完善回复、不含短路拒绝标题、站点仍走 `/chat-messages`。
不调用 Dify。

### 步骤 H5：README 与站点代理说明

文件：`README.md`

Done

写明先完善回复再内部复核、独立 Human Review KB、站点仍走 `/v1/chat-messages`。
不写入真实 Key 或 dataset UUID。

### 步骤 H6：控制台新建 Human Review KB 并发布（操作员）

操作：Dify 控制台 / 本机 API。不改 PostgreSQL。

Done

已新建高质量库 `Biosimilar Human Review KB`（id `7b9d5de8-6749-4c90-84b9-684871142da0`），嵌入 `text-embedding-v4` / `langgenius/tongyi/tongyi`，元数据 `status`、`expireAt`。已覆盖导入并发布现 Chatflow；原库检索仍绑 `Biosimilar Similarity Assistant KB`。
Dataset API Key 只写入 Dify 应用环境变量，不进仓库。Human Input 收件由操作员实机填写。

---

## 助手界面视觉升级

> 范围：只改悬浮助手外观与交互动画。不改代理、白名单、Chatflow、分析服务与上传。
> 约束：回复仍按纯文本渲染；遵守 `prefers-reduced-motion`；不把 Key 放进前端。

### 步骤 U0：写入本视觉升级步骤

Done

本段冻结为样式与空态建议问题，不改助手职责与安全规则。

### 步骤 U1：助手动效与面板样式

文件：`src/app/globals.css`

Done

新增助手面板、悬浮按钮、在线脉冲与输入中圆点的样式；动效受现有 `prefers-reduced-motion` 约束。
不改站点主色与分析页样式。

### 步骤 U2：空态建议问题与在线状态文案

文件：`src/i18n/messages.ts`

Done

增加在线状态与三条空态建议问题的中英文。
未改既有免责声明原文。

### 步骤 U3：助手徽标

文件：`src/components/assistant/AssistantMark.tsx`

Done

双螺旋线稿 SVG，供悬浮按钮、标题与气泡共用。
不含交互与网络请求。

### 步骤 U4：聊天面板视觉

文件：`src/components/assistant/AssistantPanel.tsx`

Done

渐变顶栏、在线脉冲、空态建议问题、输入中圆点与内嵌发送按钮；关闭时仍挂载以便过渡，并用 `inert` 避免误触。
回复仍为纯文本。

### 步骤 U5：悬浮按钮与面板定位

文件：`src/components/assistant/AssistantWidget.tsx`

Done

右下角改为旋转高光圆形按钮；面板绝对定位在按钮上方。
`send` 支持空态建议问题直接发问，不改请求体与密钥处理。

### 步骤 U6：Q 版看板娘

操作：右下角改为 Q 版看板娘立绘与待机动画，替换螺旋线稿图标。

Done

立绘位于 `public/assistant/mascot.png`；右下角改为漂浮、跳跃、思考摇摆与间歇对话气泡。
已删除螺旋线稿 `AssistantMark.tsx`，面板头像改为立绘脸部裁切。

### 步骤 U7：看板娘点击被挡住

文件：`src/components/assistant/AssistantWidget.tsx`、`src/components/assistant/AssistantPanel.tsx`、`src/app/globals.css`

Done

关闭态面板改为 `display: none` 且外包 `pointer-events-none`，避免透明命中层挡住看板娘；会话 id 改为发送时再读 sessionStorage，避免 hydration 不一致。
层级改为 `z-[90]`，面板贴在看板娘正上方并用 `calc(100vh - 12rem)` 限制高度。

### 步骤 U8：渐变描边盖住聊天内容

文件：`src/app/globals.css`；`scripts/verify_ui_shell.mjs`

Done

`.assistant-panel.glass-edge::before` 与抽屉层同样关掉，避免品牌蓝→珊瑚渐变铺满面板把标题、建议问题和输入框盖住。
子元素提到 z-index 3；`verify_ui_shell` 增加该选择器断言；未改代理、白名单与 Chatflow。

## 多维分层抽屉界面改造（M 系列）

### 只读复核结果（改造前实测，非假设）

- 技术栈复核：`package.json` 依赖仅 `next@16.3.0` / `react@19.2.8` / `react-dom@19.2.8`，devDependencies 含 `tailwindcss@4.3.3` 与 `typescript@5.9.3`。无任何 UI / 动画库，本次改造不新增运行时依赖。
- 路由复核：`src/app/page.tsx`、`src/app/category/[key]/page.tsx`、`src/app/item/[id]/page.tsx`、`src/app/regulatory/page.tsx`、`src/app/comprehensive-analysis/page.tsx` 共 5 个，均为「page → 客户端 View」模式。抽屉状态不进 URL，5 个路由不变。
- 布局复核：`src/app/layout.tsx` 内为 `LanguageProvider` → `SiteHeader` + `main#main-content` + `SiteFooter` + `AssistantWidget`。
- 视觉令牌复核：`src/app/globals.css` 的 `@theme` 已含 navy/brand/cyan/signal-red/canvas/paper/ink/line 全部冻结色值、`--shadow-card` / `--shadow-raised`、`--radius-sm/md/lg`；文件末尾第 445 行起已有 `@media (prefers-reduced-motion: reduce)` 块。
- 看板娘复核：`AssistantWidget` 以 `fixed right-4 bottom-4 z-[70]` 占据右下角，因此侧边交互轨必须放在视口左缘。
- 校验基线复核：改造前 `npm run check` 退出码 0（typecheck + lint + 9 个 verify 脚本全绿，lint 仅 2 条既有 `no-img-element` warning）。
- 业务文件基线哈希（SHA-256，改造后必须一致）：
  - `src/lib/comprehensive-analysis/summarize.ts` = `87c7e3c3fb7b61b34279df84858b34ac3104631970b88fe76bee22d39d2e883c`
  - `src/data/characterization-items.ts` = `57b5e99ab521b925b2e12dbffa7cd710e823ac473e419e617dd523793037c3df`
  - `src/lib/comprehensive-analysis/demo-process.ts` = `af5c92fb02069beb83813fe6f4f62cb7b5e1739439860383d1ca531b819c1ce1`

### 本次冻结的交互约定（M 系列专用）

1. 抽屉栈状态纯前端内存，不进 URL、不引入路由级抽屉，不影响 SEO 与现有 5 个路由。
2. 桌面端为「左侧交互轨 + 层叠抽屉」：每 push 一层，旧层收缩为可见层脊；层脊总宽超视口 40% 时最老层折入聚合脊。移动端（< 768px）同一 Context、同一层数据结构，改为右侧全屏推入 + 顶部返回栈。
3. 关闭规则：`Esc` 只弹一层；点击层脊回跳该深度；遮罩点击关最上层；`closeAll` 仅由显式按钮触发。
4. 焦点契约：push 后焦点移入新层首个可聚焦元素，pop 后归还触发元素；层内 Tab 焦点循环。
5. ARIA 契约：层容器 `role="dialog"` + `aria-modal="false"` + `aria-label` 为层标题；触发按钮带 `aria-expanded` / `aria-controls`；层脊按钮 `aria-label` 说明「返回第 k 层：标题」。
6. 视觉只做叠加层：不重设主色、不删改既有令牌；新增玻璃拟态、极光光晕、纵深阴影、层脊高光、stagger 与推入缓动，全部接入既有减动效块。
7. 状态字形形状差异（圆 / 八角 / 方 / 菱 / 虚线圆）为无障碍第二通道，只允许加发光与条件脉冲，不许合并形状、不许以颜色为唯一状态通道。
8. `OverallEvidencePanel` 与 `ComparisonOverview` 保持常驻主内容区，不进抽屉（沿用原步骤 9 的结论先于明细约定）。

### 免责声明清理清单（用户已明确授权）

目标状态：全站仅首页保留一行免责声明（新键 `common.siteDisclaimerLine`），其余渲染点全部移除；`src/i18n/messages.ts` 中既有 disclaimer 键的定义与原文一律不动，仅停止渲染。

需移除渲染的位置：

- `src/components/views/HomeView.tsx` 的 `site-disclaimer-heading` 区块 → 换为 Hero 正下方单行文案
- `src/components/layout/SiteFooter.tsx` 的 `common.notAGovernmentSite` 与 `referenceCase.footerDisclaimer`
- `src/components/layout/SiteHeader.tsx` 的 `common.notAGovernmentSite`（**用户清单未列出，为只读复核新发现的第 10 个渲染点**；因目标状态与 M27 断言均要求全站恰好 1 处，故一并移除，此处为对用户清单的显式补充）
- `src/components/comprehensive-analysis/OverallEvidencePanel.tsx` 的 `copy.disclaimer`
- `src/components/MethodAnalysisPanel.tsx`、`src/components/MethodContentPanel.tsx`、`src/components/MethodToolPanel.tsx`、`src/components/live-demo/MethodLiveDemo.tsx` 的 disclaimer 段
- `src/components/reference-case/ReferenceCaseSection.tsx` 的 `disclaimerTitle` / `disclaimerText` 折叠块
- `src/components/assistant/AssistantPanel.tsx` 的 `copy.disclaimer`

**冻结约定解除记录**：Dify 章节 D0 约定 7 与步骤 D10 曾把助手面板顶部免责声明列为「固定文案」，用户在本次 M 系列需求中已明确授权删除该渲染点。护栏侧不变——`src/lib/assistant/chat-request.ts` 的越权 / 整品认定 / 看图猜峰检测与 Dify 系统提示词拒绝话术一律保留。

保留项：`comprehensiveAnalysis.demoUseBanner`（`ProductPairForm.tsx` 与 `OverallEvidencePanel.tsx`）说明的是数据性质而非免责声明，语义与文案不删改，仅改为徽章呈现。

### 步骤清单

- M0 实施计划新章节（本节）
- M1 `src/app/globals.css` 新增玻璃 / 光晕 / 纵深 / 层脊 / stagger 令牌与关键帧
- M2 `src/i18n/messages.ts` 抽屉栈、层导航、交互轨、首页单行声明文案（中英成对）
- M3 `src/components/drawer/DrawerStackProvider.tsx`
- M4 `src/components/drawer/DrawerStack.tsx`
- M5 `src/components/drawer/DrawerLayerShell.tsx`
- M6 `src/components/layout/SideExplorerRail.tsx`
- M7 `src/app/layout.tsx` 挂载 Provider / Stack / Rail
- M8 `src/components/views/HomeView.tsx`
- M9 `src/components/layout/SiteFooter.tsx`
- M9b `src/components/layout/SiteHeader.tsx`（清单补充项）
- M10 `src/components/views/CategoryView.tsx`
- M11 `src/components/views/ItemDetailView.tsx`
- M12 `src/components/views/RegulatoryView.tsx`
- M13 `src/components/comprehensive-analysis/ComprehensiveAnalysisView.tsx`
- M14 `src/components/comprehensive-analysis/ComparisonOverview.tsx`
- M15 `src/components/comprehensive-analysis/OverallEvidencePanel.tsx`
- M16 `src/components/comprehensive-analysis/CategoryAssessmentSection.tsx`
- M17 `src/components/comprehensive-analysis/ItemAssessmentCard.tsx`
- M18 `src/components/comprehensive-analysis/AssessmentStatusGlyph.tsx`
- M19 `src/components/comprehensive-analysis/ItemComparisonProcess.tsx`
- M20 `src/components/MethodAnalysisPanel.tsx`
- M21 `src/components/MethodContentPanel.tsx`
- M22 `src/components/MethodToolPanel.tsx`
- M23 `src/components/live-demo/MethodLiveDemo.tsx`
- M24 `src/components/reference-case/ReferenceCaseSection.tsx`
- M25 `src/components/assistant/AssistantPanel.tsx`
- M26 `src/components/assistant/AssistantWidget.tsx`
- M27 `scripts/verify_ui_shell.mjs`
- M28 `package.json` 串入 `verify:ui-shell`
- M29 四条命令实跑 + 路由与无障碍抽查，结果回写本节

### 步骤 M0：实施计划新章节

文件：`implementation-plan.mdc`

Done

写入只读复核实测结果、8 条冻结交互约定、免责声明清理清单与 M1–M29 步骤清单。
记录 SiteHeader 为清单外新发现的第 10 个声明渲染点，并注明 D0 约定 7 已由用户解除。

### 步骤 M1：玻璃拟态、极光与纵深令牌

文件：`src/app/globals.css`

Done

`@theme` 追加三级纵深阴影、发光边框、玻璃底色与模糊、抽屉宽度与 `cubic-bezier(0.22, 1, 0.36, 1)` 缓动；新增层脊、层壳、交互轨、极光与字形发光类及 8 个关键帧。
减动效块内按类名逐一关闭全部新动画（层脊高光降为静态低透明度），既有令牌与既有类一行未删改。

### 步骤 M2：抽屉栈与交互轨文案

文件：`src/i18n/messages.ts`

Done

新增 `drawer` 段 28 个键（交互轨分组、层位置模板、层脊回跳模板、聚合脊、9 类层标题）与 `common.siteDisclaimerLine`，中英成对。
既有 disclaimer 相关键的定义与文案原文一字未改，`tsc --noEmit` 退出码 0。

### 步骤 M3：抽屉栈状态容器

文件：`src/components/drawer/DrawerStackProvider.tsx`

Done

Context 持有 `layers` 与 `pushLayer` / `popLayer` / `popToDepth` / `closeAll` / `syncLayerContent`；push 同 id 视为回跳该深度，Esc 只弹一层，触发元素按深度入栈以便 pop 后归还焦点。
`useDrawerLayerSync` 以显式 `contentKey` 驱动层内容更新，避免 ReactNode 每次渲染变化引起自循环；本文件不含任何样式。

### 步骤 M4：层叠渲染与层脊

文件：`src/components/drawer/DrawerStack.tsx`

Done

桌面端渲染层脊列 + 顶层面板，层脊按视口 40% 预算折叠为「上层 N 个」聚合脊（点击 `popToDepth(1)` 回跳）；移动端不渲染层脊，改为全屏推入 + 顶部返回栈。
push 后聚焦新层首个可聚焦元素、pop 时不抢焦点，Tab / Shift+Tab 在顶层内循环，遮罩点击只关最上层。

### 步骤 M5：单层外壳

文件：`src/components/drawer/DrawerLayerShell.tsx`

Done

层容器为 `role="dialog"` + `aria-modal="false"` + `aria-label` 层标题，头部含「第 k 层 / 共 n 层」面包屑、返回上一层与关闭全部按钮，移动端附返回栈。
表面为玻璃拟态 + 品牌蓝到青蓝 1px 渐变描边，阴影随层深从 depth-1 递进到 depth-3，层内子元素套用 stagger。

### 步骤 M6：左侧常驻交互轨

文件：`src/components/layout/SideExplorerRail.tsx`

Done

轨含 8 个大类（复用 `CategoryMark` 内联 SVG 与序号）、法规框架、全部大类索引与按 `usePathname` 派生的当前页上下文入口，每个按钮带 `aria-expanded` / `aria-controls`；桌面固定左缘垂直居中并悬停右展标签，移动端降级为顶部横向滚动条。
同时导出 `useExplorerLayers`，供后续页面复用同一套大类 / 项目 / 法规关系层构建器，避免各页重复实现层内容。

### 步骤 M7：根布局挂载

文件：`src/app/layout.tsx`

Done

在 `LanguageProvider` 内新增 `DrawerStackProvider`，依次挂载 `SideExplorerRail`（紧随 `SiteHeader`，移动端走文档流不与看板娘重叠）与 `DrawerStack`；`main#main-content` 在 md 以上留出 `--drawer-rail-width` 左侧安全边距。
metadata、`main` 的 id / tabIndex / skip-link 目标与 `AssistantWidget` 挂载位置均未改动，`tsc` 与 `eslint` 退出码 0。

### 步骤 M8：首页 Hero 与大类入口

文件：`src/components/views/HomeView.tsx`

Done

Hero 叠加极光光晕与网格视差层，四个指标改为一次性 rAF 计数（初值即真实值，故 SSR 与减动效路径直接显示终值、不闭环）；大类卡片改为 push 大类概览抽屉并保留 `/category/{key}` 详情链接。
`site-disclaimer-heading` 声明区块删除，替换为 Hero 正下方一行次级字号 `common.siteDisclaimerLine`；四项统计数字仍取自 `characterizationItems` 与既有 selectors，未新增编造数值。

### 步骤 M9：页脚免责声明移除

文件：`src/components/layout/SiteFooter.tsx`

Done

删除 `common.notAGovernmentSite` 与 `referenceCase.footerDisclaimer` 两行渲染，仅保留法规来源与英文待补说明。
页脚叠加低透明度极光层并与主内容同步左侧安全边距，海军蓝底与正文对比度不变。

### 步骤 M9b：站点头部免责声明条移除

文件：`src/components/layout/SiteHeader.tsx`

Done

删除头部 `navy-950` 声明条（用户清单外的第 10 个渲染点），使全站声明渲染点恰好收敛到首页 1 处。
skip-link、语言切换、分类菜单与移动菜单的结构与 ARIA 属性一律未改。

### 步骤 M10：大类列表页分层

文件：`src/components/views/CategoryView.tsx`

Done

项目卡片改为 push「项目速览」第 1 层（评价目的 + 指南原词），层内再 push「适用性与评价字段」第 2 层（适用性徽标、检测指标、相似性评价方法、判定原则、数值限度、备注）。
列表仍由 `getItemsByCategory(category.key)` 驱动，复用既有 `FieldBlock` / `ApplicabilityBadge` / `SupplementaryTag`，并保留跳转 `/item/{id}` 的完整详情入口。

### 步骤 M11：项目详情页分层

文件：`src/components/views/ItemDetailView.tsx`

Done

第 1 层原样嵌入 `MethodSelector`（props、内部方法选择状态与 Dify `methodId`/`methodName` 发布逻辑均未改），层内附方法清单 push 第 2 层的原理 / 演示 / 工具面板，第 2 层再 push 第 3 层参考案例与溯源。
`MethodAnalysisPanel` 仍只由 `MethodSelector` 单点渲染以免上传状态重复，进入发布 `categoryKey`/`itemId`/`itemName`、离开清理 8 个键的 effect 一字未改。

### 步骤 M12：法规框架页分层

文件：`src/components/views/RegulatoryView.tsx`

Done

在既有 `RegulatoryRelationsTable` 之下新增每条法规关系的抽屉入口，层内展示 CTD 章节、对象、关系等级与来源文件名。
表格组件的行 id 与单元格映射未改，关系等级仍用「● / ◆ / ■ + 文字」双通道表达，数据仍来自 `src/data/regulatory-framework.ts`。

### 步骤 M13：综合判别页分层入口

文件：`src/components/comprehensive-analysis/ComprehensiveAnalysisView.tsx`

Done

结论区 `OverallEvidencePanel`、总览 `ComparisonOverview` 与 `ProductPairForm` 保持常驻，八大类明细改为抽屉入口；三个固定的 `useDrawerLayerSync`（大类层 / 项目层 / 过程层）以 `sessionEpoch + itemEntries` 序列化签名驱动层内受控表单实时更新。
`summarizeComprehensiveAssessment` 的 `useMemo`、`cloneAssessmentSession` 深拷贝替换与清空 / 恢复演示逻辑一行未改。

### 步骤 M14：总览大类卡片改 push 抽屉

文件：`src/components/comprehensive-analysis/ComparisonOverview.tsx`

Done

删除 `scrollToCategory` 锚点滚动，改为必填 `onCategorySelect` 回调 push 该大类判别明细层，并补 `.tap-target` 触控目标。
五个 `inventory*` 计数字段读取、`role="meter"` 与 `aria-valuemin/max/now` 全部保持原样。

### 步骤 M15：结论区视觉升级与声明移除

文件：`src/components/comprehensive-analysis/OverallEvidencePanel.tsx`

Done

移除 `copy.disclaimer` 的 `aside` 渲染，`demoUseBanner` 改为青蓝徽章（文案与语义未改，注释标明其为数据性质说明而非免责声明）；结论改为玻璃拟态面板内的大号发光状态字形。
关键项目由锚点 `a` 改为 push 项目判别层的按钮，`aggregation` 各汇总字段读取逻辑不变。

### 步骤 M16：大类判别区适配层内渲染

文件：`src/components/comprehensive-analysis/CategoryAssessmentSection.tsx`

Done

新增可选 `onOpenItemLayer`：层内渲染时项目改为「名称 + 完成度 + 状态字形」按钮并 push 下一层，未传该回调时保持原有整卡内联渲染。
`defaultExpanded`（存在不支持 / 证据不足时默认展开）与 `createFallbackItemEntry` 兜底逻辑均未改。

### 步骤 M17：项目判别卡 push 过程层

文件：`src/components/comprehensive-analysis/ItemAssessmentCard.tsx`

Done

新增可选 `onOpenProcessLayer`：层内渲染时七步过程改为 push 下一层，未传时保留既有 `aria-expanded` / `aria-controls` 内联展开分支。
`onEntryChange` 的全部语义（适用性联动 notApplicable、状态联动适用性）与所有表单 label / required 未改。

### 步骤 M18：状态字形发光与条件脉冲

文件：`src/components/comprehensive-analysis/AssessmentStatusGlyph.tsx`

Done

新增 `emphasis` 开关：加发光，且仅「不支持相似」与「证据不足」加脉冲；`lg` 尺寸放大到 10/12 以适配大屏投影。
圆 / 八角 / 方 / 菱 / 虚线圆五种形状与旁注文字一律保留，颜色始终不是唯一状态通道。

### 步骤 M19：七步过程改纵向时间轴

文件：`src/components/comprehensive-analysis/ItemComparisonProcess.tsx`

Done

七个步骤改为带编号发光节点与渐变连接线的 `ol` 纵向时间轴，配色由 slate/teal/amber 收敛到站点冻结令牌。
「演示流程 / 尚未接入真实计算 / 不参与监管判断」三条标注与 `schematicCaption` 示意声明全部保留，示意柱状图仍标注为示意、未伪装为实测图谱。

### 步骤 M20：分析面板免责声明移除

文件：`src/components/MethodAnalysisPanel.tsx`

Done

仅删除底部 `methodAnalysis.disclaimer` 一段渲染。
上传、任务轮询、结果发布与 `AnalysisResultView` 调用链零改动。

### 步骤 M21：方法原理面板

文件：`src/components/MethodContentPanel.tsx`

Done

移除 `methodContent.disclaimer` 渲染，面板改为玻璃拟态 + 渐变描边，配色由 slate 收敛到 navy / ink / line 令牌。
「尚未嵌入字段」的显式列举保留，缺字段仍不会被误读为「不适用」。

### 步骤 M22：工具调研面板

文件：`src/components/MethodToolPanel.tsx`

Done

移除 `methodTools.disclaimer` 渲染，已调研与未调研两个分支同步换为玻璃拟态表面。
「尚未调研」的显式说明、`surveyedOn` 日期与 gapNote 提示全部保留。

### 步骤 M23：实时演示面板

文件：`src/components/live-demo/MethodLiveDemo.tsx`

Done

移除 `liveDemo.disclaimer` 渲染并改为青蓝左边框玻璃表面，`sopStillPending` 说明保留。
`getLiveDemoKind` 分派与三个演示组件的计算逻辑未改。

### 步骤 M24：参考案例区

文件：`src/components/reference-case/ReferenceCaseSection.tsx`

Done

删除 `disclaimerTitle` / `disclaimerText` 的 `details` 折叠声明块。
每个案例的监管机构 / 产品 / 文件 / 章节页码 / `evidenceLevel` 溯源字段与 `illustrative` 标注一律未动。

### 步骤 M25：助手面板

文件：`src/components/assistant/AssistantPanel.tsx`

Done

移除顶部 `assistant.disclaimer` 渲染（该冻结约定已由用户显式解除），面板加渐变描边与顶栏极光，与抽屉层观感统一。
助手回复仍为 `whitespace-pre-wrap` 纯文本渲染，未引入任何 HTML 渲染路径。

### 步骤 M26：助手挂件层级

文件：`src/components/assistant/AssistantWidget.tsx`

Done

保持 `z-[70]` 并注明层级次序（抽屉栈 60 < 左侧轨 65 < 助手 70），确保面板与看板娘不被层、层脊或遮罩压住。
看板娘底部加青蓝柔光，与玻璃 / 光晕语言一致；请求体、密钥处理与 SSE 解析未改。

### 步骤 M27：界面外壳静态校验脚本

文件：`scripts/verify_ui_shell.mjs`

Done

新增 7 组断言：免责声明渲染点恰好 1 处且位于 `HomeView.tsx` 的 `siteDisclaimerLine`、`demoUseBanner` 仍在两处渲染、状态字形仍含 5 种形状与旁注文字、`globals.css` 全部新增关键帧均在减动效块内被逐名关闭、抽屉四个组件含 `role="dialog"` / `aria-modal="false"` / `aria-expanded` / `aria-controls` / `Escape` 处理与焦点归还、`layout.tsx` 的 `main#main-content` skip-link 目标存在。
另对 `summarize.ts`、`characterization-items.ts`、`demo-process.ts` 做 SHA-256 内容哈希锁定，任何业务语义改动都会让脚本非零退出。

### 步骤 M28：串入 check 链

文件：`package.json`

Done

新增 `verify:ui-shell` 脚本并追加到 `check` 末尾，`check` 由 typecheck + lint + 9 个 verify 扩为 10 个 verify。
既有 9 个校验脚本的命令、参数与顺序一字未改，没有关闭或跳过任何断言。

### 步骤 M29：四条命令实跑与路由抽查

命令（无文件改动）

Done

四条门槛命令实测退出码均为 0：`npm run typecheck` = 0、`npm run lint` = 0（仅既有 `no-img-element` 警告，无新增）、`npm run check` = 0（10 个 verify 全过，含综合判别 10 个场景、`verify_assistant: ok`、`verify_ui_shell: ok`）、`npm run build` = 0（Turbopack 编译成功，77 个静态页面全部生成，5 条路由与 `/api/assistant/*` 均在产物中）。
`npm run dev` 下 `/`、`/comprehensive-analysis`、`/regulatory`、`/category/primary-structure`、`/item/intact-mass` 全部返回 200，且都包含 `id="main-content"`、skip-link 锚点与左侧交互轨；免责声明文案在首页出现 1 次、其余 4 条路由 0 次。键盘全流程、375px 无横向溢出与减动效归零三项本轮以源码与 CSS 静态核验为准（本机无浏览器自动化依赖，未安装以免改动依赖清单），需在浏览器中人工确认。

### 步骤 M30：抽屉层内容被渐变描边盖住

文件：`src/app/globals.css`（随后 `SideExplorerRail.tsx` / `HomeView.tsx` 同步层-1 语义）

Done

`.glass-edge::before` 原先用 mask-composite 挖空中间，叠在带 `backdrop-filter` 的抽屉层上时 Chromium 挖不空，整块品牌蓝到青蓝填充以 z-index 2 盖住层标题与正文。改为透明 padding-box + 渐变 border-box 描边，并把 `.drawer-layer` 的子元素提到 z-index 3。
左侧轨与首页大类入口在打开尚未存在的层时先 `closeAll`，保证轨上每个按钮都是第 1 层触发点，不再把多个大类概览叠成空脊。

## 暖色点缀视觉升级

> 目标：浅色冷底 + 珊瑚暖色高对比点缀，侧栏展开层白底主导、正文可读。
> 约束：不改汇总算法、上传/分析语义、演示数据逻辑；不改图表/评估结论的 emerald 成功绿、amber 警告、rose/signal-red 失败语义；不引入外部字体/图片/脚本；每次只改一个文件。

### 视觉令牌（本章冻结）

- 海军 / 品牌蓝 / 青蓝 / 信号红 / 画布 / 纸面 / 墨色：沿用既有 hex，不改色相。
- 珊瑚点缀（新增）：`#9a3412` / `#c2410c` / `#ea580c` / `#ffedd5`。白字按钮用 coral-700，悬停 coral-800。
- 抽屉层脊与层体：纸面白底 + 墨色字，禁止海军渐变作展开层主底。
- 玻璃底：不透明度 ≥ 0.96，避免侧栏透出深色遮罩导致字发灰。

### 步骤 C0：写入本章并冻结令牌与约束

Done

记录浅色冷底、珊瑚点缀、白底抽屉、语义色不可改义四条约束，作为本次配色唯一执行清单。
未改业务代码与既有冻结 hex。

### 步骤 C1：全局令牌与表面 / 抽屉 / 极光

文件：`src/app/globals.css`

Done

`@theme` 新增 coral-800/700/600/100；玻璃底不透明度提到 0.96/0.98；Hero 与极光加入珊瑚光斑；层脊与层体改为纸面白底、墨色字；轨激活态改珊瑚浅底。
未改 navy/brand/cyan/signal-red 的既有 hex；未改业务选择器。

### 步骤 C2：页头导航强调色

文件：`src/components/layout/SiteHeader.tsx`

Done

主导航激活态与底边从品牌蓝改为珊瑚；顶栏仍为海军底、白字。
未改跳转主内容、菜单结构与路由。

### 步骤 C3：页脚顶边

文件：`src/components/layout/SiteFooter.tsx`

Done

页脚顶边改为珊瑚 4px，海军底与青蓝说明文字保持。
未改监管来源文案与布局。

### 步骤 C4：首页分类卡左边线与主按钮

文件：`src/components/views/HomeView.tsx`

Done

Hero 主按钮改为珊瑚实心白字；分类卡与综合判别入口左边线改为珊瑚。
监管入口仍用青蓝左边线；未改统计数字与免责声明。

### 步骤 C5：侧栏展开层主按钮与层头不透明白底

文件：`src/components/layout/SideExplorerRail.tsx`，随后 `DrawerLayerShell.tsx`

Done

展开层主按钮改为珊瑚；层头由半透明改为不透明纸面，保证侧栏正文对比。
未改层 id、pushLayer 语义与 ARIA。

### 步骤 C6：分类页 / 项目页主按钮与页头左边线

文件：`src/components/views/CategoryView.tsx`，随后 `ItemDetailView.tsx`

Done

分类页与项目页的主按钮、页头左边线改为珊瑚；项目页方法入口卡左边线同步。
未改方法内容、分析面板与适用性徽章。

### 步骤 C7：综合判别页主按钮（不改评估条与结论色）

文件：`src/components/comprehensive-analysis/ComprehensiveAnalysisView.tsx`，随后 `ProductPairForm.tsx`

Done

恢复演示按钮、页头与大类入口卡改为珊瑚。
`ComparisonOverview` 进度条、`OverallEvidencePanel` / `CategoryAssessmentSection` 左边线、状态字形未改。

### 步骤 C8：非评估卡左边线与语言切换选中态

文件：`src/components/CategoryCard.tsx`，随后 `FieldBlock.tsx`、`LanguageSwitcher.tsx`

Done

分类卡与强调字段左边线、浅色语言切换选中态改为珊瑚。
深色顶栏语言切换仍为白底海军字；未改监管关系表与适用性徽章。

### 步骤 C9：门槛命令

命令（无文件改动）

Done

`npm run check` 退出码 0：typecheck、lint（仅既有 `no-img-element` 警告）、10 个 verify 全部通过。
未改业务哈希锁定文件；图表/评估的 emerald、amber、rose、signal-red 类名未动。

### 步骤 C10：抽屉层体改为不透明白底

文件：`src/app/globals.css`

Done

关掉 `.drawer-layer.glass-edge::before`，避免品牌蓝→珊瑚渐变铺满层体；层与层体均铺不透明纸面并设墨色字。
未改抽屉 ARIA、层栈逻辑与助手/方法卡片上的 `glass-edge`。

## 项目完整详情进抽屉层

> 目标：第三层「查看完整详情」在层栈内再推一层，展示与项目页相同的字段、方法、参考案例与分析入口，不跳路由、不关闭已展开层。
> 约束：`/item/{id}` 路由保留；抽屉状态仍不进 URL；不改汇总算法与分析语义。

### 步骤 D0：写入本章

Done

记录「完整详情进第 4 层、不关栈、保留项目页路由」约定。
未改业务代码。

### 步骤 D1：补齐完整详情层标题文案

文件：`src/i18n/messages.ts`

Done

新增 `drawer.itemDetailTitle`（完整详情 / Full detail）；英文 `viewFullDetail` 改为不暗示跳页。
未改既有键语义。

### 步骤 D2：抽出可嵌入抽屉的项目详情正文

文件：`src/components/views/ItemDetailContent.tsx`

Done

抽出字段、方法、参考案例与分析入口，方法/案例按钮继续 `pushLayer`。
不含面包屑与项目页前后导航，避免层内再跳路由。

### 步骤 D3：项目页改用抽出的正文

文件：`src/components/views/ItemDetailView.tsx`

Done

项目页保留面包屑、助手上下文与上一/下一项；正文改挂 `ItemDetailContent`。
`/item/{id}` 路由与页内语义未改。

### 步骤 D4：第三层按钮改为 push 完整详情层

文件：`src/components/layout/SideExplorerRail.tsx`

Done

「查看完整详情」由跳转 `/item/{id}` 改为 push `explorer-item-detail-{id}` 层。
已展开的大类/列表/摘要层保持在栈内。

### 步骤 D5：分类页速览的完整详情入口同步

文件：`src/components/views/CategoryView.tsx`

Done

分类页速览层的完整详情按钮同样 push 详情层，不再跳路由。
未改分类页卡片打开速览的行为。

### 步骤 D6：门槛命令

命令（无文件改动）

Done

`npm run typecheck`、`npm run lint`、`npm run verify:ui-shell` 退出码 0。
lint 仅既有 `no-img-element` 警告；未改冻结业务文件。

---

## 输入文件导入与输出报告

> 范围：在 `/comprehensive-analysis` 增加客户端文件入口。上传输入填表包后填充 61 项会话表，并生成可下载的输出书面报告。
> 性质：演示会话导入/导出。不新增后端，不写入 `AnalysisResult`，不构成监管认定。
> 约束：不修改 `src/data/characterization-items.ts`；不改变汇总算法与单项分析语义；自由文本仍不驱动判定（只解析明确的适用性/演示状态列或 JSON 字段）；每次只改一个文件。

### 冻结约定（本功能）

1. 接受 `.md` / `.txt` 填表包（A1 两列表 + A2 八列表）以及会话 JSON。未知 `itemId` 跳过并记警告。
2. 演示状态只来自「演示判定状态」列或 JSON `demoStatus`，禁止对描述字段做关键词推断。
3. 导入后用现有 `summarizeComprehensiveAssessment` 填表并汇总；输出报告复述选择器状态与录入文本，不另算科学结论。
4. 输出文件在浏览器本地下载，不持久化到服务器或图谱数据库。

### 步骤 I0：写入本章并冻结约定

Done

记录客户端导入填表包、填充 61 项会话并下载输出报告的范围与边界。
未改业务代码；汇总算法与 `characterization-items.ts` 保持只读。

### 步骤 I1：输入文件解析纯函数

文件：`src/lib/comprehensive-analysis/parse-input.ts`

Done

解析 `.md` 填表包 A1/A2 表格与会话 JSON；演示状态只取状态列/字段，描述文本不做关键词推断。
未知 itemId 跳过并记警告；空文件或无法识别时返回错误而不写入会话。

### 步骤 I2：输出报告生成纯函数

文件：`src/lib/comprehensive-analysis/build-output-report.ts`

Done

按 8 大类复述会话文本与选择器状态，总体结论取自既有汇总函数，不从描述关键词另算判定。
输出为可下载 Markdown；文件名由候选药中文名派生。

### 步骤 I3：补齐导入/导出中英文案

文件：`src/i18n/messages.ts`

Done

新增输入文件区标题、选择提示、成功/失败与下载输出按钮的中英文，未改既有键语义。
说明自由文本不驱动判定；未改汇总或分析文案。

### 步骤 I4：输入文件面板组件

文件：`src/components/comprehensive-analysis/InputFilePanel.tsx`

Done

提供带 label 的文件选择与「下载输出文件」按钮，状态区用 `role="status"`。
文件在浏览器内读取，限制 2 MB，不上传后端。

### 步骤 I5：接入综合判别主视图

文件：`src/components/comprehensive-analysis/ComprehensiveAnalysisView.tsx`

Done

页头下增加输入文件区：导入后替换会话、填充 61 项表，并立即下载输出报告。
清空/恢复仍走原按钮；下载可对当前会话重复生成。不改汇总算法。

### 步骤 I6：扩展综合判别校验脚本

文件：`scripts/verify_comprehensive_analysis.mjs`

Done

新增 Markdown/JSON 导入、关键词不改写状态、空文件错误，以及岚岫珠单抗填表包 61 项全绿汇总。
原 10 个汇总场景保留；失败仍非零退出。

### 步骤 I7：门槛命令

命令：`npm run typecheck`、`npm run lint`、`npm run verify:comprehensive-analysis`

Done

`tsc --noEmit` 退出码 0；eslint 对改动文件退出码 0；`verify:comprehensive-analysis` 15 个场景通过。
未改 `characterization-items.ts` 与汇总算法；导入后结论仍由选择器状态汇总。

### 步骤 I8：输入输出改为正式报告体例

文件：`src/lib/comprehensive-analysis/build-output-report.ts`；`docs/demo-comprehensive-assessment/` 输入数据包与比对报告

Done

下载报告与静态书面报告改为正式药学比对报告体例：总体结论、产品对、分项结果、完整度与声明；正文去掉示意/演示页眉与标签。
输入包表头改为「判定状态」；文件名改为药学比对数据包与药学相似性综合比对报告。

### 步骤 I9：文件选择器不再把 PNG 滤掉

文件：`src/lib/analysis-uploads.ts`，随后 `src/i18n/messages.ts`

Done

谱图+图片混用时清空 `accept`，避免 Windows 只显示 `.mzml` 而藏掉图谱库 PNG。
综合判别提示改为：填表包在子文件夹，根目录 PNG 走检测方法上传。

## 图谱库图进入详情输入区

> 目标：完整详情的相似性分析输入区显示本机 `图谱数据库` 中已映射的 PNG，而不是空白「暂未开放」槽。
> 约束：只提供目录中已编目的文件名；不把图拷进 git；分析仍走既有上传管线。

### 步骤 F0：写入本章

Done

记录「输入区展示图谱库图、API 只读编目文件」约定。
未改判定算法。

### 步骤 F1：按项目列出图谱库条目

文件：`src/data/figure-library-catalog.ts`

Done

按 itemId / methodId 列出已映射且 collection 为「图谱数据库」的条目。
docx-media 副本不进输入区，避免接口读不到文件。

### 步骤 F2：本机图谱库只读接口

文件：`src/app/api/figure-library/route.ts`

Done

GET `?file=` 只读编目文件名，路径限制在 catalog 的 libraryRoot。
越权路径与未编目文件名直接拒绝。

### 步骤 F3：图谱库输入区文案

文件：`src/i18n/messages.ts`

Done

补 itemPage 与 methodAnalysis 的图谱库标题、提示、一键采用与失败文案。
中英文对象字段对齐。

### 步骤 F4：完整详情输入区展示图谱

文件：`src/components/analysis/FigureLibraryInputSection.tsx`，随后 `ItemDetailContent.tsx`

Done

有编目图的项目在相似性分析区直接显示 PNG，不再只用「暂未开放」空槽。
一张图同时含候选与参照曲线。

### 步骤 F5：方法面板可一键采用图谱库图

文件：`src/components/MethodAnalysisPanel.tsx`

Done

方法面板列出本方法图谱并一键写入比对文件槽，槽内显示缩略图。
混用谱图与图片时省略 accept 属性，避免空字符串导致选中后输入仍为空。

---

## 工作台外壳移植（BioCompare 外观，不含对方运算引擎）

> 范围：把队友 BioCompare 工作台的可见框架迁入本仓库 `src/app/`。不接入对方 backend / OpenMS / Sage / FlashDeconv / Ellman fetch。
> 约束：不改 `src/data/characterization-items.ts`、`src/data/categories.ts`、`src/data/similarity-schemes.ts`；不改 `AnalysisResultView` 科学语义；不改综合判别汇总函数；不新建 `/modules/*`；不安装新依赖；不提交 `a branch/`；每次只改一个文件。

### 已核实现状（实施前只读）

- 写入目标：`D:\生物类似药判别系统\生物类似药相似性分析系统`，App Router 在 `src/app/`（禁止再创建根级 `app/`）。
- 技术栈：Next.js 16.3.0 / React 19.2.8 / TypeScript 5.9.3 strict / Tailwind CSS 4.3.3。
- 国际化：`src/i18n/LanguageProvider.tsx` + `src/i18n/messages.ts`（zh/en 成对）。
- 现有壳：`layout.tsx` 为 `SiteHeader` + `SideExplorerRail` + `DrawerStack` + `AssistantWidget`（Dify）。
- 现有路由：`/`、`/category/[key]`、`/item/[id]`、`/comprehensive-analysis`、`/regulatory`。
- 分析入口：`MethodAnalysisPanel` + `src/data/method-analysis-config.ts`；健康检查走本仓库 `ANALYSIS_SERVICE_URL` → `/api/analysis`（默认 `127.0.0.1:8765`）。
- 外观来源只读：`a branch/app/`（真正在跑的 BioCompare），不是 `a branch/src/app/`。

### 冻结实现约定

1. 首页采用对方 `/project` 工作台首页；现有 `HomeView` 不再作为 `/`。`/` `redirect("/project")`。
2. 保留 `/item/[id]` 与 `ItemDetailContent` / `MethodAnalysisPanel`。专项页只套层级条、三步条、上传卡、进度条；不新建 `/modules/[id]`。
3. 新增对方汇总报告外壳。现有 `/comprehensive-analysis` 判定逻辑、文案键、字形一律不改。
4. 整站换成对方壳（深色侧栏、顶栏搜索/主题、批量进度浮窗），同时保留中英文、现有 Dify `AssistantWidget`、现有探索抽屉。不接入对方 `AiChatWidget` 聊天后端。
5. 双左侧栏从左到右：工作台侧栏（展开约 286px / 收起约 76px）→ `SideExplorerRail` → 主区（sticky 顶栏 + children + `app-footer`）。主区宽度同时减去两者。`SiteHeader` / `SiteFooter` 从 `layout.tsx` 移除。
6. 运算接入唯一表：本仓库 `characterizationItems[].itemName.zh` 与对方 `characterizationProjects[].name` 规范化后完全相同才允许接到本仓库已有分析。规范化：去空白；去掉全角/半角括号及其中内容；去掉末尾「比对」「水平」。匹配键不用对方 `projectModules.name`。
7. 第二道闸：该 item 在 `method-analysis-config.ts` 至少有一条 `status === "analyzable"` 才真正 `createJob` / `runAnalysis`。`rule-not-defined` / `display-only` 只出框、禁用运行。
8. 不同名或本仓库没有的对方专项：只画「待接入」框，按钮禁用，文案「尚未接入本系统计算」。禁止调对方引擎，禁止用本仓库另一个项目的分析去顶。
9. 「一键执行全部比对」只调度允许名单（同名且 analyzable）；名单外不进队列。并行/串行只改批量窗文案，不改本仓库分析实现。
10. 轻量会话只允许：`filesByItemId`、`runStatusByItemId`、`progress`、`message`、`dispatchMode`、`batchRunning`、`theme`、`error`。禁止对方 `ptmResult` / `sequenceResult` 等引擎结果字段。禁止 `NEXT_PUBLIC_BACKEND_URL` 与 `127.0.0.1:8000`。
11. 侧栏「药学表征项目」用本仓库 8 大类与 61 项，链接全部 `/item/{id}`。
12. 样式两套并存：保留 `globals.css` 的 Tailwind `@theme`、抽屉、助手、综合判别；工作台规则另存 `workbench-shell.css`。深色主题只覆盖工作台壳与对方 class。
13. 全站免责声明唯一渲染点从 `HomeView` 改到 `/project` 对应视图；`verify_ui_shell.mjs` 同步改期望路径。demo banner、五态字形、抽屉 ARIA、冻结业务文件哈希不得删。

### 步骤清单

- W0 本实施计划新章节（本节）
- W1 `src/lib/workbench/name-match.ts` + `scripts/verify_workbench_shell.mjs`
- W2 `src/app/workbench-shell.css`
- W3 `src/components/workbench/CountUp.tsx`
- W4 `src/i18n/messages.ts` 工作台键
- W5 `src/components/workbench/WorkbenchProvider.tsx`
- W6 `src/components/workbench/WorkspaceShell.tsx`
- W7 `src/app/layout.tsx`
- W8 `src/app/page.tsx` redirect 与 `src/app/project/page.tsx`
- W9 概览披露 + 目录（本仓库 8 类 61 项）
- W10 `src/app/project/data/page.tsx`
- W11 `src/app/project/rules/page.tsx`
- W12 `src/app/project/report/page.tsx`
- W13 `src/components/workbench/BatchProgressPanel.tsx`
- W14 改 `ItemDetailView` 套专项框
- W15 更新 `scripts/verify_ui_shell.mjs` 免责声明路径
- W16 `npm run check` 修到全绿

### 步骤 W0：写入本实施计划并冻结约定

文件：`implementation-plan.mdc`

Done

记录外观移植范围、双左侧栏、同名允许名单两道闸、禁止对方引擎与禁止字段。
未改业务代码；`characterization-items.ts` 与综合判别汇总保持只读。

### 步骤 W1：同名允许名单纯函数与校验

文件：`src/lib/workbench/name-match.ts`；`scripts/verify_workbench_shell.mjs`

Done

规范化比较对方表征项目名与本仓库 `itemName.zh`，再与 `analyzable` 求交得到 `computableItemIds`。
校验断言人工核对的 7 个 id 必在允许名单，且名称不等的 id 不得进入 `computableItemIds`。

### 步骤 W2：抽出工作台 CSS

文件：`src/app/workbench-shell.css`

Done

从对方 globals 抽出工作台规则，去掉 html/body 重置与 AiChat 样式，深色主题只覆盖工作台壳。
增加双左侧栏宽度变量与探索轨偏移，并在本文件减动效块中关闭工作台动画。

### 步骤 W3：数字动画

文件：`src/components/workbench/CountUp.tsx`

Done

移植对方 CountUp，尊重 `prefers-reduced-motion` 与 IntersectionObserver。
未新增依赖。

### 步骤 W4：工作台中英文案

文件：`src/i18n/messages.ts`

Done

新增 `workbench` 段中英成对文案，覆盖侧栏、顶栏、首页、输入、规则、报告、专项框与批量窗。
未改既有键语义；免责声明单行文案写入工作台首页键。

### 步骤 W5：轻量会话 Provider

文件：`src/components/workbench/WorkbenchProvider.tsx`

Done

会话只保存文件槽、运行状态、调度模式、主题与错误；`runItem` 非允许名单直接返回。
允许名单走本仓库 analysis-service 客户端，不读 `NEXT_PUBLIC_BACKEND_URL`。

### 步骤 W6：工作台壳

文件：`src/components/workbench/WorkspaceShell.tsx`

Done

还原品牌行、项目卡、四链加综合判别与法规、八大类折叠导航与顶栏工具区。
药学表征分组用本仓库 8 类 61 项并链到 `/item/{id}`；未挂对方 AiChatWidget。

### 步骤 W7：根布局换壳

文件：`src/app/layout.tsx`

Done

globals 之后引入工作台 CSS；用 WorkbenchProvider 与 WorkspaceShell 包主区。
移除 SiteHeader / SiteFooter；保留探索轨、DrawerStack 与 Dify AssistantWidget。

### 步骤 W8：首页重定向与工作台首页

文件：`src/app/page.tsx`；`src/app/project/page.tsx`

Done

`/` 重定向到 `/project`。工作台首页落地英雄区、整套上传外壳、五步骨架、编排条、流程线、任务历史与 KPI。
免责声明唯一渲染点改到该页；上传只按扩展名表面分类，不解析内容、不调对方引擎。

### 步骤 W9：概览披露与表征目录

文件：`src/components/workbench/ProjectOverviewDisclosure.tsx` 及相关目录/手风琴/分析框组件

Done

视觉抄对方概览与目录 class，数据改为本仓库 8 类 61 项，卡片链到 `/item/{id}`。
已接入手风琴只列允许名单，展开嵌入与专项页相同的分析框。

### 步骤 W10：统一输入页

文件：`src/app/project/data/page.tsx`

Done

61 项分页 15；仅允许名单文件槽可写会话，其余禁用并标等待接入。
未做对方 mzML 快捷分配、PTM 切换、FASTA 槽与模板下载。

### 步骤 W11：公共规则页

文件：`src/app/project/rules/page.tsx`

Done

画出参数、风险色条与三条已锁定政策；值只存在本页 state。
明确不传入 MethodAnalysisPanel、不改 similarity-schemes、不改 analysis-service。

### 步骤 W12：汇总报告页

文件：`src/app/project/report/page.tsx`

Done

落地 5 个 KPI、按本仓库八大类折叠表、结论边界卡、风险卡与登记表。
穿透链接到 `/item/{id}`；Word 导出按钮禁用；保留不自动给出生物类似性结论。

### 步骤 W13：批量进度浮窗

文件：`src/components/workbench/BatchProgressPanel.tsx`

Done

只列出本会话 `source === "batch"` 的允许名单项，文案走 i18n。
并行/串行只改变窗体文案，不改变本仓库分析实现。

### 步骤 W14：专项页外套工作台框

文件：`src/components/views/ItemDetailView.tsx`

Done

增加层级条并保留知识字段、方法选择与参考案例。
分析区外套对方 panel；允许名单继续挂 MethodSelector，其余禁用运行并给出待接入空结果骨架。

### 步骤 W15：校验脚本免责声明路径

文件：`scripts/verify_ui_shell.mjs`

Done

唯一免责声明渲染点期望改为 `/project` 页与 `messages.workbench.siteDisclaimerLine`。
demo banner、五态字形、抽屉 ARIA、冻结哈希检查保留。

### 步骤 W16：全量校验

文件：`package.json` 及相关排除配置

Done

`npm run check` 串入 `verify:workbench-shell`；tsconfig 与 eslint 排除 `a branch/`。
修复到全绿。

### 步骤 W17：揭开方法面板被渐变层盖住的正文

文件：`src/app/globals.css`；`scripts/verify_ui_shell.mjs`

Done

`.glass-surface.glass-edge::before` 与抽屉/助手同样关闭，方法原理、实机演示与工具调查不再被 brand→coral 色块盖住。
校验脚本锁定该选择器，避免回退。

### 步骤 W18：程序分析面板在服务未连上时自动重试健康检查

文件：`src/components/MethodAnalysisPanel.tsx`；`src/components/workbench/WorkbenchProvider.tsx`

Done

未连上时每 4 秒再打 `/api/analysis/health`，本仓库 FastAPI 起来后红框与侧栏引擎状态会自行恢复。
仍只走本仓库 8765 转发，不读对方 8000 后端。

