# P4 contract + P5 FastAPI service runtime + P6 secure ingest + P7 intact-mass
pydantic>=2.13,<3
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
python-multipart>=0.0.9
defusedxml>=0.7,<1
Pillow>=10,<13

# P7 scientific stack (optional in Docker slim image; required for intact-mass adapter)
numpy>=2,<3
matplotlib>=3.8,<4
pyopenms>=3.5,<4
unidec>=8.2,<9

# P10 image-fallback stack. Headless OpenCV avoids pulling a GUI toolkit into the
# service image. pytesseract is a wrapper only and needs the Tesseract binary
# installed separately; the eng traineddata is sufficient for the figures in scope,
# whose axis labels and tables are entirely English.
opencv-python-headless>=4.10,<6
scikit-image>=0.24,<1
pytesseract>=0.3.13,<1

# tests
httpx>=0.27,<1
pytest>=8,<9
pytest-asyncio>=0.24,<1
