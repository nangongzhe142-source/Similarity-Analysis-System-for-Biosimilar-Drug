# PTM/MAM OpenMS + Sage 正式预期与验收清单

版本：R0.1  
维护分支：03｜PTM区间与位点定量  
适用范围：原始 `mzML + FASTA` 经 OpenMS + Sage 形成合格PTM位点定量结果，再进入 BioCompare 多批参照药区间引擎。

## 1. 固定职责边界

- OpenMS + Sage负责目标/诱饵库、谱图搜库、PSM打分与FDR处理。
- 上游定量组件负责经验证的XIC峰提取、积分、归一化和质量控制。
- 输出转换器负责将合格鉴定和定量结果映射为BioCompare PTM输入合同。
- PTM区间引擎只消费已鉴定、已通过质量门槛且已完成定量的结构化结果。
- PTM区间引擎不替代搜库、FDR、修饰定位或XIC定量。
- 系统只标记客观偏离，不自动输出“相似”或“不相似”结论。

## 2. 任务状态及允许用途

| 状态 | 含义 | 是否可产生区间结果 | 是否可进入审评汇总 |
| --- | --- | --- | --- |
| `queued` | 已排队，尚未执行 | 否 | 仅显示状态 |
| `running` | 外部链路执行中 | 否 | 仅显示状态 |
| `awaiting_quantification` | 鉴定质量门槛通过，但尚未形成合格XIC位点定量 | 否 | 仅显示待完成门槛 |
| `awaiting_interval` | 合格PTM定量表已形成，但区间引擎尚未完成 | 否 | 仅显示待计算状态 |
| `quality-blocked` | 程序完成，但鉴定、定量或系统适用性未通过质量门槛 | **否** | 仅显示阻断原因和诊断证据 |
| `failed` | 程序、输入、超时或适配器执行失败 | 否 | 仅显示失败原因 |
| `completed` | 所有预设质量门槛通过，且区间引擎成功形成客观结果 | 是 | 是，但不得形成自动相似性结论 |

强制规则：

1. `quality-blocked`不得转换为`completed`，不得生成或复用任何诊断性区间结果。
2. `/quality-gate-result`只用于审计和故障诊断，不得作为正式区间结果接口。
3. 只有`completed`任务才能向总项目回传PTM区间摘要。
4. 放宽FDR、复用候选样本或使用技术代理定量形成的结果必须明确标记为诊断结果，并与正式结果物理隔离。
5. 可选FragPipe/MSFragger上游遵循`PTM-FRAGPIPE-UPSTREAM-CONTRACT.md`；其返回码0和`awaiting_downstream`均不表示PTM比较完成。

## 3. 当前移交任务的固定预期

任务ID：`b72d65c8896e4f41bd2dbc3f60ebbdc5`

| 检查项 | 固定预期 |
| --- | --- |
| 任务终态 | `quality-blocked` |
| 阶段 | `quality-gate` |
| 目标PSM | 1,019 |
| 诱饵PSM | 857 |
| 最低谱图q值 | 0.02739726 |
| 1% FDR通过目标PSM | 0 |
| 合格PTM位点定量记录 | 0 |
| `comparison` | `null` |
| 区间结果接口用途 | 不得作为正式结果 |
| 允许用途 | 验证异步链、审计产物和质量阻断机制 |

该任务必须持续作为负向回归样本：未来代码变更后，如果它产生正式区间结果或进入总项目PTM结果汇总，回归测试应判定失败。

## 4. 正式输入前置条件

### 4.1 实验设计

- 参照药来自可追溯的独立生产批次；不得把同一样品的技术运行当作独立批次。
- 候选药与参照药样品身份独立，禁止复用同一mzML充当两组样品。
- 批次、制样、酶切、仪器运行和技术重复关系具有明确元数据。
- 采用适用于目标生物药的高分辨率MAM采集方法和预先冻结的方法参数。

### 4.2 系统适用性

- 系统适用性样品、接受标准和失败处理预先定义。
- 质量精度、保留时间、信号响应、峰形、重复性和携带污染达到方法要求。
- 系统适用性失败的运行不得进入鉴定定量或区间构建。

### 4.3 鉴定与FDR

- 目标/诱饵策略、数据库版本、酶切规则、固定/可变修饰和质量容差被记录并冻结。
- PSM层面q值不高于0.01；如采用肽段或蛋白层额外FDR，阈值和计算层级必须明确。
- 输出保留PSM标识、序列、修饰形式、蛋白归属、运行标识、目标/诱饵标签及q值。
- 不得用未过滤Sage结果或诊断模式结果填充正式PTM输入。

### 4.4 修饰定位

- 位点特异性修饰必须有经验证的定位算法和预设定位概率阈值。
- 无法唯一定位的修饰不得被静默指定到某一残基。
- 输出保留定位概率、候选位点集合和定位失败原因。

### 4.5 XIC定量

- 正式定量使用经验证的MS1/XIC峰面积或已批准的等效定量方法；不得使用MS2强度技术代理。
- 记录提取离子窗口、保留时间窗口、积分边界、同位素选择、干扰处理及归一化方法。
- 输出包含定量值、定量单位、LOQ、缺失值原因、积分质量标记和技术重复标识。
- 共洗脱、错误峰选择、信号低于LOQ和严重干扰必须触发不可定量或质量警告。

## 5. BioCompare PTM输出合同最低字段

每条正式记录至少需要：

- `lot_id`
- `protein_chain`
- `residue`
- `position`
- `modification`
- `value_percent`
- `replicate_id`
- `quant_status`
- `loq_percent`
- `identification_q_value`
- `localization_probability`
- `risk_level`

同时应在任务级保留：原始文件SHA-256、FASTA哈希、软件真实自报版本、参数、命令、返回码、时间戳、操作者/项目、系统适用性结果及转换器版本。

## 6. 多批区间引擎验收

- 技术重复先在独立生产批次内汇总，不能增加参照药独立批次数。
- 参照药批次数不足时输出`insufficient_reference`，不得强制构建区间。
- 区间方法和最低参照批次数必须在任务执行前冻结并记录。
- 候选药逐位点输出区间内、高于上限、低于下限、证据不足或无法判断。
- 候选药存在而参照药不存在的修饰按唯一分析物标记`candidate_only_variant`。
- 检测单批参照药、位点选择性批次子集和数据缺失，形成完整性预警。
- 候选药与参照药中位数差仅作辅助信息，不替代区间符合性。

## 7. 正式回归场景

### 7.1 顶层原始PTM文件束自动分流

顶层统一入口必须先完成文件束结构和身份检查，才允许切换到OpenMS + Sage异步模式。

#### 应自动切换异步模式的正向场景

- 文件束仅包含一个FASTA。
- 至少包含公共规则要求数量的参照药mzML，并包含一个或多个候选药mzML。
- 每个mzML的候选药/参照药角色可以无歧义确定。
- 每个参照药文件能够关联到可追溯的生产批次身份；技术重复另有独立标识。
- 文件类型、扩展名、大小、哈希和角色校验均通过。

#### 必须保持部分就绪、禁止自动运行的负向场景

- 上传多个FASTA，或无法确定唯一使用的FASTA。
- mzML的候选药/参照药角色缺失、冲突或无法识别。
- 参照药mzML数量低于公共规则要求。
- 多个参照药mzML实际来自同一生产批次，但缺少技术重复关系说明。
- 候选药与参照药文件哈希相同，疑似复用同一原始文件。
- 文件角色可识别，但生产批次、制样或技术重复元数据不完整。

自动分流只证明“文件束适合提交到上游异步链”，不证明文件束已通过系统适用性、FDR、定位概率、XIC定量或PTM区间门槛。

#### 文件束身份最低记录

每个mzML至少记录：

- 原始文件名和SHA-256；
- 样品角色：`reference`或`candidate`；
- 独立生产批次ID；
- 制样ID；
- 技术重复ID；
- 仪器运行ID；
- 采集时间和方法版本；
- 上游样品清单或用户确认来源。

参照药最低批次数必须按不同的生产批次ID统计，不能按mzML文件数量统计。

### 7.2 必须通过的下游正向场景

- 独立多批参照药和候选药均通过系统适用性、1% FDR、定位概率与XIC定量门槛。
- 可构建参照药逐位点区间并回传`completed`。
- 区间内、区间外、数据不足和新增修饰均按预期计数。

### 7.3 必须阻断的下游负向场景

- 1% FDR无通过PSM。
- 只有技术重复、没有足够独立参照批次。
- 候选与参照复用同一原始文件。
- 修饰定位概率不足或无法唯一定位。
- XIC低于LOQ、积分失败或严重干扰。
- 系统适用性失败。
- 输出表缺少必填字段、数值越界或批次身份无法追溯。

## 8. 正式放行条件

只有同时满足以下条件，才能考虑将监管工作流状态改为已验证、将生产状态改为可用：

1. 独立多批高分辨率MAM代表性数据完成正向与负向回归。
2. 1% FDR、定位概率、XIC定量和系统适用性规则均经过方法学确认。
3. OpenMS、Sage及转换器版本冻结，解决Sage资产标签与程序自报版本不一致问题。
4. 任务审计产物、失败恢复、超时终止和结果下载完成验证。
5. PTM区间引擎的批内汇总、区间构建、数据漏洞和新增修饰预警完成固定预期测试。
6. 审评报告仅呈现客观证据状态，并保留人工审评结论入口。

在上述条件全部满足前，状态必须保持：

- `technicalConnectivityValidated = true`
- `regulatoryWorkflowValidated = false`
- `productionAvailable = false`
