# BioCompare 生物类似药比对项目平台

这是一个独立实现的本地原型。平台采用“顶层总项目＋下层独立分项任务”的结构：顶层统一管理输入、公共参数、风险规则和报告，下层分别执行专项计算并将结果回传至项目汇总。

## 已实现

- 统一大项目“生物类似药比对项目”及项目级启动入口。
- 完整分子量、脱糖完整分子量、轻链分子量、未脱糖重链、脱糖重链和LC-MS翻译后修饰位点六个独立分项入口。
- 一级总页面统一申报材料入口：一次上传多份CSV、TSV、XLSX及待后续OCR处理的PDF/Word/图片材料。
- 基于文件名、工作表名、字段名和样品角色字段自动识别专项、拆分候选药/参照药、规范化字段并分发至六个专项。
- 自动解析结果显示识别依据、就绪/缺失/冲突状态和解析预警；不静默猜测无法确认的数据。
- 可执行分项的顶层批量调度、独立状态管理与结果回传。
- 项目级公共材料、分项输入分配、公共参数和风险规则界面。
- 分项证据登记和统一审评报告汇总框架。
- 可运行的确定性PTM区间引擎：支持CSV/TSV、多批参照区间、技术重复批内汇总、逐位点落区、新型变体及选择性批次预警。
- 指导原则一级结构项目及关联重链糖型项目的统一展示。
- 5 个分子质量项目的共同分析入口。
- A药/B药两列峰表的确定性对比与可视化。
- A药/B药原始两列 m/z–强度文本谱的 UniDec CLI 去卷积适配器，支持独立进程、超时终止、退出码和输出文件校验。
- 匹配峰、质量差、ppm差、相对丰度差和研发筛查分。
- 输入与运行请求哈希、引擎版本和免责声明。
- 独立 FastAPI 后端、持久化任务队列、阶段进度和事件记录。
- 原始文件、UniDec结果、AI/规则摘要、完整结果包及日志下载。
- 总项目汇总页一键导出整体 Word（DOCX）结果，包含项目摘要和各专项关键数据。
- OpenAI兼容的大模型接口；未配置密钥时使用不杜撰数据的本地规则摘要。
- 内置元数据清单、分子质量结果和肽段鉴定标准数据包模板。

## 启动

在 PowerShell 中运行：

```powershell
& ".\scripts\start-local.ps1"
```

打开 <http://localhost:3000>。

后端接口文档位于 <http://127.0.0.1:8000/docs>，启动脚本会同时启动网页和后端。

## 配置AI模型

复制 `.env.example` 为 `.env.local`，填写兼容 Chat Completions 接口的模型地址、密钥和模型名：

```text
AI_BASE_URL=https://your-provider.example/v1
AI_API_KEY=your-key
AI_MODEL=your-model
```

不要把包含密钥的 `.env.local` 上传到GitHub。没有配置时，系统仍会完成UniDec分析，并明确显示“本地规则摘要”。

UniDec任务默认最长执行600秒。可在 `.env.local` 中通过 `UNIDEC_TIMEOUT_SECONDS` 调整，允许范围为10至3600秒；超时后任务会终止并进入失败状态，错误信息保存在任务日志中。

## 输入文件

UniDec入口目前接受两列数值文本：第一列为 m/z，第二列为强度。PDF、Word、Excel、mzML、FASTA和鉴定结果等可作为补充证据上传、保存和下载，但不会被错误地送入UniDec。真实项目中还应增加仪器原始格式转换、OpenMS/搜索引擎工作流、样品处理元数据、方法模板锁定、OCR/视觉模型和系统适用性检查。

PTM区间引擎分别接收参照药和候选药CSV/TSV定量表。必填字段为 `lot_id`、`protein_chain`、`residue`、`position`、`modification` 和 `value_percent`；详细说明和示例位于 `public/templates/`。区间方法必须在运行前选择，当前支持多批观测范围、均值±3SD和稳健MAD。最低参照药批次数是项目参数，不代表统一法规阈值。

一级总页面的统一材料入口当前直接解析CSV、TSV和XLSX。识别规则包括文件名/工作表中的专项名称与候选药、参照药标识，以及表内 `sample_role`、`m/z`、`intensity`、PTM位点字段等。PDF、DOCX和图片可以随整套材料上传并留存，但当前只生成“等待OCR/文档解析”的提示，不会伪造结构化结果。若同一质量项目识别到多个同角色数据表，系统标记冲突并要求人工确认，不会自动选取其中一份。

## 当前引擎状态

- UniDec 8.2.1：已安装并通过独立CLI适配器接通后台任务。
- OpenMS/pyOpenMS：当前机器尚未检测到有效安装，原始谱图的肽段搜索和PTM鉴定仍由外部上游软件完成。
- BioCompare PTM Interval 0.1：已接入，处理已完成鉴定和定量的修饰位点结果，不负责原始谱图搜索。

## 重要限制

此原型仅用于研发初筛和软件架构验证，不能作为放行、注册申报或监管相似性结论。正式使用前需要真实方法数据验证、权限与审计、电子签名、备份恢复和计算机化系统验证。
